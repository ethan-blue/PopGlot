.alma-snapshots/
.vs/
.vscode/
.idea/
target/
**/bin/
# Rust developer CLI binaries are source, not build output.
!crates/popglot-core/src/bin/
!crates/popglot-core/src/bin/*.rs
**/obj/
*.user
*.suo
*.cache
*.log
artifacts/

# Ad-hoc local dumps from debugging sessions (CI log captures, zip experiments,
# redirected test output, accidental Windows device-name files).
joblog*.txt
jobs.json
rel.zip
test_output.*
NUL

# Local secrets and provider overrides must never be committed.
appsettings.local.json
*.secrets.json

# Local publish output
dist/
.alma/
