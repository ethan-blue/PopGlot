# PopGlot UI 完整评审报告（2026-09-03）

评审范围：15 个 XAML（≈3,570 行）全部全文审读 + 34 个未提交改动 diff 评审 + 计划文档对照。
验证方式：资源 key 全量比对、95 个事件处理器 ↔ code-behind 全量比对、Binding 属性 ↔ 数据模型比对、构建 + 120 项逻辑测试。
基线：docs/DESIGN_SYSTEM.md (v0.1.1+)、docs/UI-REFACTOR-PLAN.md、HEAD c64f26e。

## 总体结论

- 构建干净（0 警告 0 错误）、120 项逻辑测试全过、资源引用零缺失、事件处理器零缺失。
- "UI 完全有问题"的根因：**TranslationPanelWindow.xaml 结果卡 Grid 结构错误（已修复，见下）**。
- 计划文档四轮任务的完成度无虚报；但当前工作区是"第五轮"改动（无计划文档覆盖），问题集中在这一轮。

## 🔴 严重问题（1 处根因，2 个表现）——已修复 ✅

**TranslationPanelWindow.xaml 结果卡**：
1. 文本层 Grid（原 303 行）缺 `Grid.Row`，与引擎标题栏（原 238 行）同落 Row 0 → 译文文字压在"译文"徽章和朗读/复制/收藏按钮上，按钮无法点击。
2. 说明/保护术语/警告区（原 330 行）被挤到 0 高度 → 语境解析、Token 词条、失败警告永远不可见。
3. 长译文把 Auto 行撑爆固定 420px 窗口 → 内容被裁切。

修复：结果卡 Grid 增加 Auto 行；文本层挂 `Grid.Row="1"`（占 * 行，内部滚动，code-behind 的 FindScrollViewer/ScrollToEnd 流式跟随逻辑不受影响）；说明区挂 `Grid.Row="2"` 并加 `MaxHeight="200"` 上限。参照 TranslateSection.xaml:147 的正确模式。

## 🟡 已一并修复 ✅

- LibrarySection.xaml:36 搜索框 Height 34→38：原内容区仅 14px，13px 中文行高 ≈17px，文字上下被裁切。
- ServicesSection.xaml:298 字号 10→11：低于 Metadata 11 下限，125% DPI 下发虚。

## 🟡 待修复（26 项中等，按优先级）

### P1 布局/行为
- App.xaml.cs:60-71,140-155 全局吞异常屏障无条件 Handled=true 且无节流 → UI 回归静默带病运行 + 托盘通知轰炸（正是本次问题被掩盖的原因之一）。
- TranslationPanelWindow.xaml:5 固定 580×420 无 MinWidth/MinHeight，DPI/无障碍大字号时内容只被裁切。
- QuickSearchWindow Ctrl+R=朗读 vs 翻译浮窗 Ctrl+R=重试：一键二义。
- GeneralSection.xaml:56-57 "失焦自动关闭浮窗"说明文案自相矛盾（把 Pin 的两行文案误用为设置行 hint）。
- PrivacySection.xaml:55-60 "恢复询问"（管理操作）混在"允许/拒绝"（授权操作）旁，建议视觉分离。

### P2 一致性（重构拆分后"散掉"的部分）
- 壳标题字号三种：15（主窗侧栏）/ 19（设置侧栏）/ 20（PageTitle）→ 应统一 20。
- "译文"字号三种：14（库详情）/ 14.5（工作台）/ 15（浮窗+查词+FlatRichTextBox）→ 应统一 14.5。
- metadata 字号 11/11.5 混用约 15 处、横跨 5 文件 → 统一 11。
- IconButton 五档尺寸并存（24×22/26×26/28×26/30×30/32×32）；28×26 出现在主窗口违反"主窗 ≥32"承诺（TranslateSection:78,85,99 等）。
- 徽章圆角 4/6/7 三种 → 统一 6 或胶囊。
- 状态栏字号 11.5（主窗）/ 12（设置窗）不一致；侧栏宽 168 vs 176。
- ServicesSection 字号散失最重：10/11/11.5/12/12.5/13.5/14/15/16/18 十档；:279,336 的 18、:189 的 15/16 均无刻度依据；:228 的 13.5 回归了第三轮"13.5→13"的承诺。
- ServicesSection:211-218 圆角 9 不在 6/10/12 体系；:519 DraftGuardBar 圆角 8 应为 10；:114 Padding 1.5px 脱离 4/8 网格。
- PrivacySection:119,129 12.5px 配 20 行高（1.6 倍）超出 1.45-1.54 补偿区间。
- TranslateSection 输入 14 / 译文 14.5 双栏不对称；:56,130 手写 FieldLabel 样式 ×2 未收拢。
- QuickSearchWindow:47 搜索字号 16 无 token；:98 "[未完成]" 方括号风格与全 UI 去装饰原则不一致。
- CaptureOverlayWindow:36-46 `#F2FFFFFF` 内联 ×5 未命名（Task 4 只把十字线提了资源，白色没提）。
- Controls.xaml:1389-1394 关闭钮红色硬编码（有 Windows 惯例豁免理由，建议至少命名注释）。

### P2 语义/存量兼容
- ProfileManager.cs:81 Ollama 改全角括号后，旧版（≤0.1.2）ASCII 括号名不再判 pristine → v4→v5 清理迁移对存量配置失效。
- "生成中"（静态 pill）vs"正在生成…"（reducer BadgeText）两套语态并存。
- SettingsWindow 保存按钮 ToolTip"即时生效"与实际（进行中请求保持旧快照）略不符。

## 🟢 记录在案（24 项轻微，摘要）

魔法数（PhoneticText MaxWidth=240、EngineSummary 320、列表列 300 等，均有 Trim/ToolTip 兜底）；chevron 几何 3 份拷贝未提资源；DropShadowEffect 纯黑 ×4（token 体系缺 Shadow）；ToggleSwitch/SwitchCheckBox ~60 行重复（有注释豁免）；死资源 ModelRecommendationChipStyle；死代码 MinHeight=34 on ComboBox（ServicesSection:154,166）；FloatingTrigger 点击区 30×30 略低于 32 建议；DataSection 清空按钮缺两步确认 ToolTip（Library 有）；NavPrivacy 用相机图标语义牵强；文档漂移（测试数 39/51/68/113 各处打架，实际 C# 87 + Rust 112；Sections 行数表全部过期；DESIGN_SYSTEM.md 与计划 §十三的 Canvas/Accent 色值互相矛盾；schema 实际 v6 文档停在 v5；csproj 用 PopGlot-v3.ico 但 make-ico.ps1 仍产出 PopGlot.ico）；ServicesSection.xaml.cs 2114 行正在长成新 god class（原 MainWindow 才 1791）；工作区杂物 joblog.txt/joblog2.txt/jobs.json/rel.zip 应删或 gitignore。

## 已验证无问题（避免误修）

全部 StaticResource/DynamicResource 引用有效（0 缺失）；95 个事件处理器全部存在；Binding 属性全部存在于数据模型；GeneralSection/ShortcutsSection/DataSection 完全 token 化；P0 级历史修复（标题栏图标、ClearType、去 MessageBox）保持到位；SettingsWindow 与 MainWindow 职责切分干净无死代码；Ui.cs 是活的基础设施（8 个 XAML 依赖）；本轮 Rust 侧三处修复（is_local_base_url 严格判定、SSE 256KiB、请求 id 消歧）均为真实安全修复；渲染矩阵测试按存量限制输出透明帧——这是它拍不出视觉重叠的原因，建议后续升级为真实合成。

## 修复记录（本次会话 · 两批）

### 第一批（会话内直接修复）
| 文件 | 改动 |
|---|---|
| TranslationPanelWindow.xaml | 结果卡 Grid 补第三行；文本层 Grid.Row="1"；说明区 Grid.Row="2"+MaxHeight=200；注释同步 |
| LibrarySection.xaml | 搜索框 Height 34→38 |
| ServicesSection.xaml | "常用中转/私有端点" 字号 10→11 |

### 第二批（🟡 全量批量修复 + 卡死修复）
| 范围 | 改动 |
|---|---|
| 字号统一 | 壳标题统一 20；译文统一 14.5（含 FlatRichTextBox 样式）；metadata 11.5 全部归 11；ServicesSection 10/13.5/15/16/18 归刻度；浮窗输入 13.5→14 |
| Ctrl+R 二义 | QuickSearch 朗读改 Ctrl+P（tooltip/快捷键处理/页脚提示同步），Ctrl+R 全局保留重试语义 |
| GeneralSection | "失焦自动关闭浮窗"说明文案改为描述开关开启行为，消除绕口令 |
| PrivacySection | "恢复询问"与允许/拒绝视觉分离 |
| ProfileManager | IsPristineTemplate 兼容 ≤0.1.2 旧 ASCII 括号名「Ollama (本地)」（新增 LegacyOllamaTemplateName） |
| 文案语态 | "生成中"/"正在生成…"统一；SettingsWindow 保存按钮 ToolTip 改准确 |
| 一致性 | 徽章圆角归 6；StatusDot 圆角统一；IconButton 主窗口 ≥32；侧栏宽度统一 168；ServicesSection 圆角 9→10、8→10、Padding 1.5→2 |
| 资源 | chevron 几何收拢 IconChevronDown 共享；关闭钮红色提命名资源；CaptureOverlay 覆盖白提命名资源；删除死资源 ModelRecommendationChipStyle、死 MinHeight |
| 卡死修复 🔴 | **UI 线程裸调 Clipboard.SetText 三处全部接入 STA 工人 + 硬超时通道**（Helpers.CopyToClipboardAsync / QuickSearchWindow.Copy_Click / MarkdownPresenter 代码块复制，新增 WindowsSelectionClipboardAdapter.TryWriteTextAsync）。剪贴板被其他程序占用时 UI 不再冻结，失败时提示"剪贴板被占用，未复制" |
| 测试 | 新增结构断言：translation panel result card rows keep text and explanation separated（防 Grid.Row 重叠回归） |
| 撤案 | App 崩溃屏障复核：节流+落盘已存在（15s 窗口 1 次气泡 + 静默计数），评审 P1-3 为误报 |

最终状态：构建 0 警告 0 错误，**121 项逻辑测试全过**（+1 结构断言）。
