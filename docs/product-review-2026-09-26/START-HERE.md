# 交给其他 AI 的执行入口

将下面整段复制给接手 AI。完整规格见同目录 `AI-MODIFICATION-SPEC.md`。

```text
你正在维护 D:\Projects\GitProjects\PopGlot。
请先阅读 docs/product-review-2026-09-26/AI-MODIFICATION-SPEC.md，按其中任务与依赖实施。
该审查基线为 419db04b96377a5de36bdb0ab76662c79dfa1f8a；先检查当前 HEAD、git status、diff 和适用 AGENTS.md，保留既有改动。代码和可重复证据优先于历史文档中的 DONE 标签。

第一批只做 R10、R01、R06：建立当前事实索引，修复要点缓存/异步结果归属，修正文档与隐私披露。完成后交付改动、实际测试结果和剩余验证项。不要把后续所有建议一次性重写进产品。
R01 先用可控假执行器复现同原文切目标语言、请求晚到和切引擎场景；缓存键与实际请求快照必须一致。兼顾工作台和浮窗，不清空译文掩盖问题。
R06 对照实际代码核对凭据路径、公共服务接收域、历史敏感形状检测、撤销授权和在途取消，删除无证据的绝对承诺。

遵守现有设计系统和行为约定，不进行全量 UI/MVVM 重写，不新增付费调用或遥测，不读取真实密钥与私人历史，不关闭正在运行的 PopGlot，不提交/推送/打 tag/发布。真实 Provider 测试需要另外明确授权。
必要代码修改完成后执行适用测试；最终运行现有 verify 门禁前确认 WPF 隔离条件。外部命令检查退出码，不使用固定测试数量判断成功。
报告逐项使用“通过 / 不通过 / 未验证”，分清纯逻辑、WPF 隔离、真机、真实服务和发布包。旧截图或 mock 不能替代真实验证；环境不足则保留未验证项并给出精确步骤。
```

后续批次按主规格第 4 节推进。新增安装器、签名、自动更新、外部账号和发布行为不包含在第一批授权中。
