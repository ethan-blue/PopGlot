# 对账F：N10–N18 功能规划 vs 现实

> 对账日期：2026-09-14　执行者：Pi（Teammate，slot 01a09fee-a79e-7f62-a233-8374e07ada67）
> 合同依据：docs/review-2026-09-12/PRODUCT-EXPANSION-REVIEW-V2.md §5（N10–N18 条款）
> 台账现状（EXECUTION-LEDGER.md §2）：九项均标 SPECIFIED_NOT_IMPLEMENTED
> 方法：只读分析——逐条读 V2 §5 原始合同 → 对照当前代码（apps/PopGlot.Windows）与 2026-09-13 UI 大修（W1–W18）波次产物。未跑 GUI/测量/测试，未做 git 操作。

## 判定总览

| ID | 主题 | 判定 | 兑现来源 |
|---|---|---|---|
| N10 | 服务诊断与费用透明 | **PARTIAL** | 既有架构 + W2（SS-02/03）+ W14 |
| N11 | 首次无服务体验 | **PARTIAL** | W2（SS-04）+ W3（WIN-01）+ 免费引擎授权链 |
| N12 | 设置搜索与保存语义 | **PARTIAL** | V02/V03 返修 + W5（SS-12/13）+ W13 |
| N13 | 专注模式与快捷键暂停 | **NOT_STARTED** | — |
| N14 | 布局密度、显示器记忆与缩放 | **PARTIAL（兑现度最高）** | W7（WIN-10/11/12/13）+ W12 + W13 |
| N15 | 翻译质量验收与用户纠正 | **NOT_STARTED** | — |
| N16 | 最近任务、长文进度与请求预算 | **PARTIAL** | 分段会话既有实现（C20 关联） |
| N17 | 第二服务复译 | **NOT_STARTED（L3）** | —（合同定位为后续保留功能） |
| N18 | 文件翻译与学习扩展 | **NOT_STARTED（L3 研究级）** | —（合同明示不进入当前开发队列） |

统计：PARTIAL×5，NOT_STARTED×4（其中 L3×2），DONE×0。
**台账建议**：N10/N11/N12/N14/N16 五项应从 SPECIFIED_NOT_IMPLEMENTED 修订为"部分实施（波次/架构覆盖，缺口见本报告）"；N13/N15 维持未实施；N17/N18 维持未实施并保留 L3 层级标注。

---

## 逐项判定

### N10 当前服务、连接诊断与费用透明【L1 状态，L2 扩展】—— **PARTIAL**

**已兑现（含证据）**：

| 合同条款 | 现实证据 |
|---|---|
| 翻译结果上显示实际使用服务 | `TranslationCoordinator.cs:287,319,732`（PipelineLabel：本地模型/内置免费引擎/具体 Provider）；展示于 `TranslationPanelWindow.xaml.cs:789`（EngineBadge）、TranslateSection 的 TranslateEngineBadge（TranslateSection.xaml:449）、`QuickSearchController.cs:156` |
| 菜单切换只影响下一次请求 | 请求创建时捕获 draftSettings 快照（`TranslationCoordinator.cs:89-105`），在飞请求与后续切换隔离；引擎切换已异步化+防重入（W14，`MainWindow.xaml.cs:445,493`） |
| 默认保存配置不联网；测试连接/获取模型各有明确按钮 | 独立按钮 `TestConnectionButton`/`FetchModelsButton`（`ServicesSection.xaml.cs:514-547` 门控、1054-1102、1505-1548）；无 Key 前置拦截（W2 SS-02/03）；成功文案明示"草稿未保存…连接测试只报告健康状态，不是使用前提"（`ServicesSection.xaml.cs:1526-1529`） |
| 健康结果只作参考，不作为永久可用证明 | 会话级 outcome（null=尚未验证），`DescribeProfileState` 注释明示 "never presented as permanent health"（`ServicesSection.xaml.cs:652-679`） |
| 失败分类（认证/限流/超时等基础档） | `ClassifyTestFailure`：auth(401)/rate(429)/endpoint(404)/unreachable(超时/未知主机/SSL/TLS)（`ServicesSection.xaml.cs:683-712`）；列表态显示"鉴权失败/限流/服务不可达"等（658-671） |
| 验收：模型目录失败仍允许保存手填模型；未知模型写能力待验证 | 手填项以 Unknown/Unknown 能力入列（`ServicesSection.xaml.cs:1330` Fallback descriptor）；能力计数"支持 X / 不支持 Y / 未知 Z"（1400-1401）；`ModelCatalogService.cs:19+` 无模态信息一律 Unknown，不发明能力 |
| 自动切付费服务默认关闭 | 全库不存在自动切换付费服务机制（免费引擎也需显式授权，见 N11），空缺即合规 |
| 手工代理（合同明示"可选后续"） | 未做——按合同不算缺口 |

**缺失子项**：
1. **取消按钮缺失**：测试连接与获取模型进行中仅禁用按钮+超时/异常兜底（`ServicesSection.xaml.cs:1425-1431` 处理已取消/超时文案），无显式取消操作。合同："各有明确按钮与取消"。
2. **健康结果无时间戳**：测试成功只显示 host/HTTP 状态/耗时 ms（1515-1526），无检测时间。合同："健康结果带时间"。
3. **高级诊断未分层**：DNS、连接、TLS 全部折叠进 "unreachable" 单档，无安全建议展示；合同要求区分 DNS、连接、TLS、认证、限流、超时。
4. **费用透明整体未做**：全库无价格/usage 展示，连"费用由服务商结算"的兜底文案都没有（grep 费用/价格/usage/计费 在 ServicesSection、ModelCatalogService 均为空）。
5. 验收项"任务切服务不改已发送请求"无显式测试守卫（快照隔离是设计事实，未见专项断言）——列入验证债。

### N11 首次使用与无服务体验【L1】—— **PARTIAL**

**已兑现（含证据）**：

| 合同条款 | 现实证据 |
|---|---|
| 首次主窗直接展示可操作工作台（非阻塞） | 主窗无任何首启向导/模态引导，即开即用 |
| 无服务就地给"配置服务"入口 | W2 SS-04：工作台空态引导卡"尚未配置翻译引擎"+"添加翻译引擎"主按钮（`TranslateSection.xaml:367-400` UnconfiguredGuidePanel）；极速查词无服务错误卡+"打开设置"（W3 WIN-01，`QuickSearchWindow.xaml:160-183`） |
| 首次真实翻译前展示实际接收方与发送范围 | 内置公共翻译须先显式授权，授权即展示去向："首次翻译会连接 translate.googleapis.com"（`TranslateSection.xaml.cs:927`）；ToolTip 完整发送范围"…与 clients5.google.com；不发送截图、历史记录或密钥"（`TranslateSection.xaml:421`） |
| "仅提取文字"能力存在 | 托盘"截图提取文本 (OCR)"+Shift+截图（`App.xaml.cs:1223` BeginCapture(ocrOnly:true)） |
| 不引导用户开启所有隐私开关 | 授权均为单点显式同意（FreeEngineConsent/CloudSpeech），空态卡只推"添加引擎/使用内置公共翻译"两个动作 |
| 输入草稿在前往配置再返回时保留 | 设置为独立窗口，主窗输入区不销毁（自然保留；无显式守卫测试） |

**缺失子项**：
1. **三步非阻塞首启引导**（输入方式、当前可用服务、快捷键试用；可跳过；引导隐藏后可从帮助重开）——全库无 FirstRun/Onboarding 逻辑（grep 首次/引导 在 MainWindow/TranslateSection/App 仅命中免费引擎授权文案）。
2. **离线演示模式**（本地预置输入/输出、醒目标"演示，未联网"、不进真实历史、不展示伪造耗时）——无任何实现。
3. 空态引导卡无"仅提取文字"入口（OCR 只能经热键/托盘触达，无服务用户不一定知道该路径）。
4. 帮助系统整体缺失——"引导隐藏后可从帮助重开"无载体。
5. 五条路径验收（无 Key、无 OCR、无网络、拒绝授权、已有旧配置）未做过系统性走查——验证债。

### N12 设置搜索、修改语义与恢复默认【L2】—— **PARTIAL**

**已兑现（含证据）**：

| 合同条款 | 现实证据 |
|---|---|
| 显式保存（安全、启动、快捷键、服务修改） | 表单级 Loading/Clean/Dirty/Saving 状态机（`SettingsWindow.xaml.cs:16-30,62`）；服务编辑器独立草稿守卫（W5 SS-12：编辑器打开时收拢底栏保存条，消除双保存条）；切页/关窗守卫（428,766） |
| 修改再改回 → clean | 草稿与基线值比较收敛（`RecomputeStateFromDraft`/`StateFromDraft`，`SettingsWindow.xaml.cs:216-218`；V03 注释明示 "a form rolled back onto the baseline returns to Clean"） |
| 保存过程只禁用提交相关操作，显示进行中 | Saving 态仅锁保存路径（200,302-313 "正在保存…"），输入不受限 |
| 校验先于提交、失败回滚不混淆 | 校验段（快捷键冲突预检保留旧注册："快捷键注册失败…未保存任何修改"，525-529）→ 提交段（策略写失败恢复原快捷键；shell 写失败回滚全部，558-585） |
| 自启写失败与其他设置逐项呈现（V02/V03） | 自启保存走 Run-only 执行器+失败基线回写+保留重试意图（555-605；`StartupRegistration.cs:376,420-422`） |
| 服务草稿"显式保存或放弃" | DraftGuardBar + "已放弃未保存的修改。"（`SettingsWindow.xaml.cs:660`） |

**缺失子项**：
1. **设置搜索完全未做**：设置窗无搜索框（grep Search 在 SettingsWindow.xaml(.cs) 为空），无中文名/别名匹配、无定位到行。
2. **"恢复本页默认"未做**：全库无 RestoreDefault/恢复默认（grep 为空），遑论"先形成可查看的草稿、不删数据、不清 Key、不扩大授权"。
3. **版本号防旧异步写覆盖未做**：ShellSettings.SchemaVersion=3 仅是迁移版本（`ShellSettings.cs:232`），非保存并发护栏；W9 异步化（ProfileManager/注册表 Task.Run）后，旧异步写晚到覆盖新状态的风险无版本机制防护（现靠保存串行化+回滚缓解，未见断言）。
4. **每行生效时机标注未做**：仅全局"设置已保存，即时生效。"（607）与个别"重启 PopGlot 后生效"（ServicesSection:1968）文案，无逐行标注。
5. **主题预览未做**：ThemeCombo 变更仅标脏（139），保存后才切换；"可预览但退出草稿要还原"整条无实现。
6. 验收矩阵（保存失败/连续改语言/关闭设置/应用退出/主题预览还原/组合键冲突）部分被 LogicTests（V02/V03/A01）覆盖，其余未系统化执行。

### N13 专注模式与快捷键暂停【L2】—— **NOT_STARTED**

- 托盘菜单全量为：打开/极速查词/恢复最近翻译/翻译选中/截图翻译/截图提取/设置/退出（`App.xaml.cs:1207-1228`），**无"暂停快捷键 15 分钟/直到手动恢复"**。
- 无专注模式、无全屏检测（grep 暂停快捷键/SuspendHotkeys/PauseHotkey/15 分钟/直到手动 均为空）。
- 录制快捷键临时暂停与用户主动暂停的分离计数、睡眠跨截止时间不误恢复等合同点全部无对应代码。
- 唯一"暂停快捷键"字样是崩溃熔断的故障处理文案（`App.xaml.cs:425` "已暂停快捷键与新任务"），属 C 系熔断合同，不是用户功能。
- 通知纪律（仅明确需要处理的失败才通知、后台完成不抢焦点、全屏不自动弹主窗）：面板就地更新、托盘通知现用于最小化提示与故障提示，方向大体相符，但无合同化实现与验收——随本项一并未做。

### N14 布局密度、显示器记忆与缩放【L1】—— **PARTIAL（九项中兑现度最高）**

**已兑现（含证据）**：

| 合同条款 | 现实证据 |
|---|---|
| ≥960 左右分栏增强 | W12：`TranslateSection.SetWide`（`TranslateSection.xaml.cs:405-409`，1:1.25 宽屏双栏） |
| 720–959 标准侧栏 | `MainWindow.xaml.cs:584-601` 断点注释与实现（168 DIP 侧栏） |
| 560–719 侧栏折叠+上下阅读 | W7：`SetStacked(clientWidth < 720)`+侧栏 48 DIP 图标模式（`MainWindow.xaml.cs:596-601`；`TranslateSection.xaml.cs:434-438`） |
| 断点以客户区宽度判定（G07 口径） | SizeChanged 按客户区宽度统一判定（`MainWindow.xaml.cs:584-601`） |
| 低于 560 硬下限 | MainWindow MinWidth=560（`MainWindow.xaml:6`） |
| 设置窗窄窗单列收缩 | W13：`SettingsWindow.xaml.cs:484-485` SetCompact + `GeneralSection.xaml.cs:39-53`（标签在上、控件占满行宽） |
| 多屏/DPI：查词窗光标屏居中+限高 | W7 WIN-10（`QuickSearchWindow` WindowStartupLocation 改光标屏工作区） |
| 悬浮球物理像素→DIP+屏幕夹逼 | W7 WIN-11（`FloatingTriggerWindow.xaml.cs` + `ScreenGeometry`） |
| 定位 Gap/Edge DPI 修正+工作区夹逼 | W7 WIN-13（`WindowPositioner.cs:42-50` ClampToRange） |
| 浮窗拖动后停止自动追随 | `_userMoved` 标记（`TranslationPanelWindow.xaml.cs:1521-1527` "A dragged panel must stop chasing its anchor"；PositionNearAnchor 短路） |
| 长语言名不截断 | 语言下拉用完整 DisplayName（`LanguageCatalog.cs:13-15,46`），无"自动检/简体中"式截断 |

**缺失子项**：
1. **用户字体放大未做**：正文默认字号大致在 12.5–14.5 阶梯（W5 归一后），但"允许用户放大至 200%"与"字体放大与显示器 DPI 分开测试"均无实现（全库无 FontZoom/TextScale 设置）。
2. **源/译文可拖动分隔比例未做**：工作台输入|结果区无 GridSplitter、无最小值约束、无"重置布局"（grep GridSplitter 在 TranslateSection/MainWindow 为空；W6 的 GridSplitter 在资料库分栏，非本合同对象）。
3. **主窗位置持久化未做**：`ShellSettings`（215-233 行）无任何窗口位置/显示器标识字段，主窗每次默认位置启动——"保存逻辑矩形和显示器标识、恢复前夹到工作区、拔屏/任务栏改变后重定位"整条缺失。
4. <560 的"以工作区边界为准降级"仅靠 MinWidth 硬下限；极小工作区（如 150% DPI 小屏）下可能溢出，无显式降级策略。
5. 验收债：560/720/960/1440 × 短/长/错误内容、125/150/200% DPI、文本缩放、拔屏、任务栏方向变化、Tab 焦点顺序——全部未做真机走查（断点数值本身也未经"实测内容可读性确认"）。

### N15 翻译质量验收与用户纠正【L2】—— **NOT_STARTED**

- **人工审阅语料集未建**：`tests/fixtures/prompts/` 13 项与 `docs/TRANSLATION_BENCHMARK.md` 是流式解析/token 保护/结构保真的**性能与鲁棒性基准**，非"人工审阅的质量语料集"（无每类≥10 条的人工评分、无标识符保真与自然语言准确度分开评分体系）。属相邻基础设施，不构成兑现。
- **"编辑译文"本地人工版本**：全库无 EditTranslation/人工版本 逻辑（grep 为空）——保留模型原始版本、人工版标识、撤销/导出均无。
- **纠正按钮/本地备注/复制诊断**：无任何代码。

### N16 最近任务、长文进度与请求预算【L2】—— **PARTIAL**

**已兑现（含证据）**：

| 合同条款 | 现实证据 |
|---|---|
| 长文预算与拒绝原因（输入过大/单代码块超限） | `TranslationCoordinator.cs:900-910`：PlanSegments 拒绝时给"最多 X 段、每段 Y 字符，请缩短或分批提交"与"oversized_code_block 请缩短该代码块"两条可操作文案 |
| 顺序分段+总 deadline 覆盖所有段 | 919-926：链接 CTS + `CancelAfter(SessionDeadline)` 统一截止 |
| 第 X 段失败/取消不发送第 X+1 段 | 循环头部 `ThrowIfCancellationRequested`（937）+ 异常即终止循环 |
| 已完成部分保留为 Partial、不虚假报成功 | 960-978：`session.TranslatedText = merged` + "第 X 段翻译失败，仅保留已完成片段"；未完整段即以 Partial 终止（"following segments would glue onto an unreliable fragment"） |
| 停止能力 | 面板 Esc 阶梯首次取消运行中请求（`TranslationPanelWindow.xaml.cs:1355-1368`，A06/C05）+ CancelOperation/重试（293-303） |
| 单交互任务、无后台无限队列；新提交替换旧任务 | 单会话顺序执行；epoch 防过期回写（`TranslationCoordinator.cs:205+`，请求创建捕获 epoch） |
| 不显示虚假剩余秒数 | 无任何剩余时间估算显示（合规——但也无任何进度显示，见缺口） |

**缺失子项**：
1. **长文进度 UI 缺失**：会话阶段枚举（Created/AcquiringInput/OcrRunning/Routing/Translating/Streaming/Finalizing/Completed/Partial，`TranslationModels.cs:11-21`）无分段维度——"第 X/Y 段、已完成部分"不显示（"第 X 段"字样仅在失败警示中出现）。
2. 拒绝原因分类未收全：输入过大/代码块超限已细分，**输出超限**未见专项拒绝路径；服务限流有用户文案（`FreeTranslateService.cs:455-458` 429 冷却提示）但未进入统一"拒绝原因"框架。
3. 暂停/恢复：合同要求段级输入快照+幂等合并+权限复核后才提供——未提供属合规，但对应的**"显式重试未完成部分"恢复路径**也未做（现有"重试"是整段重发）。
4. **最近任务列表未做**：仅托盘"恢复最近翻译"单面恢复（`App.xaml.cs:1098-1123` RestoreRecentSurface，属 N01 会话面恢复谱系），无任务列表/状态/预算占用展示。

### N17 结果对照与第二服务复译【L3】—— **NOT_STARTED（L3，合同定位为后续保留）**

- 全库无复译/对照/第二服务代码（grep 复译/第二服务/对照翻译/重译 为空）。
- 合同本身规定"作为后续效率功能保留""等 N10 完成后再立项"（V2 §7.2）——N10 现为 PARTIAL，故本项未启动**不构成违约**，如实标注层级即可。

### N18 文件翻译与学习扩展候选【L3 研究级】—— **NOT_STARTED（L3，合同明示不进入当前开发队列）**

- 无 TXT/Markdown 批量翻译、文档对照、生词复习代码（grep 文件翻译/TXT 为空）。
- 合同要求"每项先独立立项"，当前亦无任何 ADR/评估文档记录候选清单评审——候选状态本身未管理，如实记录。

---

## 缺失子项汇总（按优先级建议排序）

**P1（日用刚需/合同 L1 残留）**
- N13 整项：托盘暂停快捷键（15 分钟/手动恢复）+ 通知纪律
- N14：主窗位置持久化（逻辑矩形+显示器标识+恢复夹逼）；用户字体放大至 200%
- N16：长文分段进度 UI（第 X/Y 段）；"显式重试未完成部分"路径
- N10：测试连接/获取模型取消按钮；健康结果时间戳；DNS/连接/TLS 诊断分层+安全建议

**P2（L2 合同主体）**
- N12：设置搜索；"恢复本页默认"草稿；保存版本号护栏；逐行生效时机
- N11：三步非阻塞首启引导+帮助重开；离线演示模式（"演示，未联网"）；空态卡"仅提取文字"入口
- N15：人工审阅语料集（负责人资源决策）；"编辑译文"本地人工版本；纠正按钮/本地备注
- N10：费用透明（可信价格+usage 才展示，含"费用由服务商结算"兜底文案）
- N14：源/译文分隔比例拖动+最小值+重置布局

**验证债（实现已有但无验收证据）**
- N10："切服务不改已发送请求"专项断言；N11 五条路径走查；N12 保存语义验收矩阵补全；N14 断点真机走查（560/720/960/1440 × 125/150/200% DPI、拔屏、任务栏方向、Tab 顺序、混合 DPI、睡眠锁屏）；N16 分段边界零丢字断言

## 建议下一步（≤3）

1. **合并小波次清 N10/N12 的低成本缺口**：取消按钮+健康时间戳+诊断分层（N10）与设置搜索+恢复默认草稿（N12）互不依赖文件冲突面小，可一波完成，直接把两项推向接近 DONE。
2. **N13 单独立项**：托盘"暂停快捷键 15 分钟/直到手动恢复"（截止时间模型+录制暂停分离+睡眠跨期处理）是独立用户功能，文件面集中在 App.xaml.cs/HotkeyService/ShortcutsSection，一波可交付。
3. **N14 收尾+验证债打包**：主窗位置持久化与字体放大实现后，把 560/720/960/1440 × DPI 真机走查与 N11 五条路径、N10 快照隔离断言合并成一次真机验收轮（与对账A/C 报告的真机矩阵债共用场次）。

## 备注

- 本报告为只读分析产物；未改动台账 §2 状态行（修订建议见"判定总览"，待负责人确认后统一执行）。
- 交叉引用：N14 与对账G（UI01–UI07/GAP 表）高度重叠，本报告只按 V2 §5 功能合同口径判定，UI 视觉口径以对账G 为准；N16 分段与对账D C20 相邻（分段预算在 Rust core），本报告只覆盖 C# 会话层进度呈现。
- 波次证据来源：docs/ui-audit-2026-09-13/README.md（W1–W15 映射表）+ 看板 W16–W18 返修记录 + 本轮代码实测（file:line 以 2026-09-14 工作树为准）。
