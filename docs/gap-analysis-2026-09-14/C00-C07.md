# 对账 A：C00–C07 验收状态升级判定 + 验证债清单

> 2026-09-14。执行者：Pi（teammate，只读对账）。
> 对象：`docs/review-2026-09-12/EXECUTION-LEDGER.md` §2 中标 READY_FOR_REVIEW 的 C00–C07。
> 合同：第一轮 `MASTER-PRODUCT-REVIEW-AND-EXECUTION-PLAN.md` §8 + `POST-C00-C07-REVIEW-AND-CONTINUOUS-PLAN.md`（A01–A12、V01–V06、四轮 V 退回）。
> 方法：只读分析。未改产品代码/测试/台账，未跑 GUI/测量脚本，未做 git 写操作。证据来源＝台账追加记录 + 磁盘测试日志/构建产物时间线 + 生产源码抽查（A01–A10 修复均已在当前源码核实到位）。
> 本报告判定为"候选"建议；DONE_VERIFIED 签字权在负责人（G02：独立复核者签收同一构建）。

## 1. 前置阻塞解除确认

台账 §2 各行仍写"套件执行待实例退出/待实例退出"。该阻塞已于 2026-09-13 实际解除，证据链（磁盘可核）：

| 证据 | 内容 | 来源 |
|---|---|---|
| 全量套件 3 轮全绿 | `bin/LogicFinal/run1~3.log`：**180 passed, 0 failed ×3**（09-13 18:19–18:21；含 `real user config unchanged by the run`、`no unsanctioned public network send was attempted` 全 PASS） | W17 记录 + 磁盘日志 |
| 追赶回归过程 | `bin/LogicFreezeHunt*/`：164/16 → 167/13 → 168/12（暴露 W8/W3 真实回归后由 W16/W17 闭环） | 磁盘日志 |
| 最终状态验证 | 台账末条（用户故障返修）：**LogicTests 185/185 + PureTests 18/18 + Rust workspace 180/180 + Release/Debug 0 警告 0 错误 + 隔离 `--settings` 启动实测（进程响应/设置窗可创建/0 crash log/词库哈希不变）** | 台账 2026-09-13 |
| 三项目编译 | app + LogicTests + PureTests Release 0 警告 0 错误（W16–W18、末条记录一致声明） | 台账各条 |
| 独立复核存在 | lead 四轮 V 退回（V01–V06、C09×3）+ R01–R06 更正 + ReleaseLeadCheck 亲自抽查构建（09-13 15:53）+ W16–W18 紧急返修指令 | 台账 + bin 目录 |

构建身份：工作树已清空并提交为 **HEAD 9e4832a（0.1.5，2026-09-13 22:15）**；`bin/Release` 于 22:14 重建（与 Program.cs 最终改动 22:13 对应），台账末条 185/185 覆盖最终状态——但该轮未保留原始日志文件（见验证债 D7）。

**结论：升级判定的执行证据前提成立。** §2 的"待实例退出"证据列已过时，升级后需同步刷新（由台账维护者执行，本报告不代改）。

## 2. 逐项判定表

| ID | 判定 | 一句话依据 | 升级条件 |
|---|---|---|---|
| C00 | **DONE_VERIFIED 候选** | 验收三条款全闭合：实例在场 exit 3 单错误（2026-09-12 真机实证）；无实例全量 3×180/0 + 185/185；配置哈希不变（套件内建测试逐轮 PASS） | 无（建议随 D7 一轮 HEAD 复跑留档） |
| C01 | **DONE_VERIFIED 候选** | A04 线性化闭环（先核验后原子消费、消费点移至构建后提交前、once-only 拒签发后 Denied、fallback 间撤销停发、并发恰一 send）；pure 8 场景 + 全量套件 + W18 原子性复验 | 无（真实网络 E2E 归 C23，列债 D9） |
| C02 | **DONE_VERIFIED 候选** | A08 有界读取/严格 UTF-8/损坏只读/快照重试 + V04 UI 重试入口已接线（源码核实）+ W16 写失败可见/并发不丢；超限 fixture 哈希不变、收藏显式失败、并发不丢更新全绿 | 无（列债 D5/D6′） |
| C03 | **DONE_VERIFIED 候选** | A09 结构化条目 + V01 四轮收敛：栈帧仅来自运行时结构化 API（源码核实，自由栈文本默认省略）；注入矩阵 + 轮转预算 + 后台写 Flush 全绿 | 无（导出入口不存在＝据实"不适用"，未来义务列债 D6） |
| C04 | **DONE_VERIFIED 候选** | A07 围栏符号/长度追踪（四反引号含内三反引号逐字符一致）；7 探针 + 三入口一致（W17 修复回归后全量绿）；LF 规范化合同已明示 | 无（跨应用剪贴板 E3 列债 D4） |
| C05 | **DONE_VERIFIED 候选（附条件）** | 状态机/接线/隔离级行为全绿：隐藏完成不碰剪贴板、恢复不补发、查词失焦保留会话、Esc 先菜单/IME 后取消、最近使用恢复、强关真销毁 | E3 真机失焦矩阵（IME/Alt+Tab/系统通知/他应用）**不签通过**，挂债 D1；§6.5 该部分保持"未验证" |
| C06 | **DONE_VERIFIED 候选（附条件）** | 泛型 InvalidOperation 不再一律可恢复、RuntimeGate 熔断拒绝新任务（入口+热键双保险）、重启先清旧资源后交互斥体；分类/熔断/拒绝新工作套件全绿；真机崩溃风暴（20260913 真实故障）已修并实测 | E3 注入演练 + 真实重启交接（V05 真机）挂债 D2 |
| C07 | **DONE_VERIFIED 候选（附条件）** | A01–A03 闭环：PlanSaveAction 普通保存绝不写禁用、BuildRunCommand/ExtractExecutablePath 命令合同、OsDisabled 三态诚实状态、marker 托盘就绪后消费、V03 基线磁盘真相+重试锁（四轮收敛）；8 状态决策矩阵 pure+套件全绿 | 真机临时账户登录 20 次 20/20 挂债 D3 |

统计：**8/8 为 DONE_VERIFIED 候选**；其中 C00–C04 无保留条件，C05/C06/C07 附"E3 子条款保持未验证并挂账"条件。无一项建议保留 READY_FOR_REVIEW——因为各任务的**可自动化验收范围**已在当前构建全绿且经独立复核史（lead 四轮退回→逐轮收敛）覆盖；剩余 E3 子条款与本批实现质量无关，环境就绪前保留在任何状态都无法推进，挂"验证债"比压状态更诚实。

## 3. 验收条款逐条对账（条款 → 证据 → 状态）

### C00 测试隔离与可信基线（P0）

| 条款（第一轮 §8） | 证据 | 状态 |
|---|---|---|
| 有真实实例时 1 个环境失败、0 个后续测试 | 2026-09-12 真机实证：PID 32548 在场 → exit 3 单一错误；带 POPGLOT_TESTS_FILTER 同样拦截（台账 C00 行） | ✅ 通过 |
| 无实例时全量单进程连续通过 | LogicFinal 3×180/0（磁盘日志）+ 台账末条 185/185 | ✅ 通过 |
| 前后真实配置哈希不变 | `real user config unchanged by the run` 逐轮 PASS（run1~3.log 第 188 行） | ✅ 通过 |

第一轮 F12（155/13 与实例冲突的 12 个 Application 生命周期失败）已随 STA/Application 单一所有者（A11 RunStaBatch 结构）在全量绿中消解。

### C01 免费引擎最终发送授权（P0）

| 条款 | 证据 | 状态 |
|---|---|---|
| F01 撤销后健康请求 sends=0 | pure `AuthorizationLinearization`：签发→Denied→send=0 且未消费（源码：`OutboundPolicy.cs:59-66` 先实时政策核验后 `Interlocked.Exchange`） | ✅ 通过 |
| F02 AllowOnce 复用 sends=1 | pure：并发双 claim 恰 1 send；send 失败已消费不返还；cache hit 0 send | ✅ 通过 |
| F03 fallback 期间撤销 sends=1 后停 | pure：fallback 前撤销→不再发 | ✅ 通过 |
| 并发双消费测试 | W18 复验原子消费 + `failures==1` 断言；全量套件 `no unsanctioned public network send` PASS | ✅ 通过 |
| 消费点在 send 提交边界 | 消费移至 URL/header 构建后、transport 提交前；构建失败不烧许可（EndpointsOverride seam 可测） | ✅ 通过 |

语义收窄已记录：LiveSettingsLoader 缺失回退快照仅限纯/测试宿主，生产在 App 启动绑定（源码断言保留）。

### C02 词库超限、损坏与写入恢复（P0）

| 条款 | 证据 | 状态 |
|---|---|---|
| 超大 fixture 操作后原文件字节/哈希不变 | ReadBounded 单句柄累计上限（`VocabularyStore.cs:542`，读中增长不可穿透）；套件 `vocabulary store protects unreadable files from destruction` PASS | ✅ 通过 |
| 收藏返回明确失败 | W16：写失败内存回滚 + `LastPersistError` + "未保存到本机，请重试"（UI 源码核实）；套件 `save failures stay visible` / `panel star failure is visible` PASS | ✅ 通过 |
| 正常并发收藏不丢更新 | 套件 `vocabulary concurrent changes do not overwrite each other` PASS（W16 修复单写者队列时序） | ✅ 通过 |
| 损坏只读 + 隔离副本 + 重试 | Corrupt 进 LoadBlocked；QuarantinedSafely 需副本哈希校验；RetryLoad 快照-提交（`LibrarySection.xaml.cs:118-131` UI 重试按钮已接线＝V04 闭环） | ✅ 通过 |
| UI 展示"未加载，新收藏未保存" | LibrarySection 分状态文案（超限/占用/无权限只读保护）源码核实 | ✅ 通过 |

### C03 诊断最小化与脱敏（P0）

| 条款 | 证据 | 状态 |
|---|---|---|
| source:/api_key=/自定义秘密/Bearer/URL query/用户路径矩阵不可恢复 | pure `diagnostics entries are structured and secret-free` + 套件 `crash diagnostics sanitize secrets and bound length` PASS | ✅ 通过 |
| 结构化 allowlist、不写自由 message/StackTrace | `DiagnosticsLog.cs:200-207`：帧仅来自 `new StackTrace(exception, fNeedFileInfo:false)` 运行时结构化 API（V01 四轮收敛；"at synthetic_secret_123" 型自由文本不可信） | ✅ 通过 |
| 日志查看/导出前复扫 | 当前产品无导出入口——据实记录"不适用"；SanitizeForExport 留作未来入口强制复扫边界 | ✅ 据实（未来义务 D6） |
| 轮转/并发有界 | 套件轮转预算测试 + 后台单写者线程（W9，1024 队列）+ Flush 测试 PASS | ✅ 通过 |

### C04 代码与结构保真（P0）

| 条款 | 证据 | 状态 |
|---|---|---|
| 7 探针逐字符断言 | pure `markdown plain text keeps fenced code byte-exact` + 套件 `markdown plain text preserves technical identifiers and code` PASS | ✅ 通过 |
| 四反引号/语言标签/空代码块/尾换行/混合 prose+code | A07：ClassifyFenceLine/ClosesBlock 追踪 opener 符号+长度（`MarkdownPresenter.cs:108-143`），确定性 fixture（外四内三反引号）三路径一致；反引号 info 含反引号不算围栏 | ✅ 通过 |
| 不整体 Trim/不 TrimEnd 代码行；复制与 TTS 分离 | A07/旧 C04 改动保留；W17 复验三入口复制一致（`three entries copy the same agreed plain text` 回归已修，全量绿） | ✅ 通过 |
| CRLF/LF 输出合同 | LF 规范化合同在注释与断言中明示："除换行规范化外保真" | ✅ 合同明确 |

### C05 关闭、失焦、取消与恢复状态机（P0/P1）

| 条款 | 证据 | 状态 |
|---|---|---|
| 关闭入口统一（X/Alt+F4/关闭热键=取消+隐藏） | `CloseAsUserIntent`（`TranslationPanelWindow.xaml.cs:1586`）+ 套件 `focus-loss and close contracts are wired through the shell` PASS | ✅ 通过 |
| 隐藏期副作用禁止（自动复制等） | gate.WindowVisible 纳入 ShouldTriggerAutoCopy + 剪贴板提交边界复查 IsVisible；套件 `hidden panel completion never touches the clipboard` PASS（可见对照恰一次、恢复不补发） | ✅ 通过 |
| Esc 先菜单/IME 后取消，终态隐藏 | 视觉树探测 ContextMenu.IsOpen；查词以活跃 _cts 为准任意阶段取消；套件 `A06 escape precedence recency and exit wiring` PASS（隔离级模拟） | ✅ 通过（隔离级） |
| 失焦矩阵：输入法/系统通知/Alt+Tab/其他应用 | **未验证（E3）**——隔离级仅覆盖同进程弹层（ForegroundBelongsToThisProcess 过滤，W17 修复）与反射触发 Deactivated；真机矩阵待环境 | ⚠️ 验证债 D1 |
| 在途/完成/失败/partial 内容可恢复且无副作用 | 会话状态/epoch 栅栏/partial 保留组套件全绿；查词失焦保留会话 PASS | ✅ 通过 |
| 恢复按最近使用 | _panelLastUsedUtc/_quickSearchLastUsedUtc 元数据（A06）；N01 多会话仓（5 会话/2MiB/30 分钟）**未实施**，属 N01 独立任务，不得读作随 C05 完成 | ✅ 通过 / 边界注明 |

### C06 全局异常策略（P1）

| 条款 | 证据 | 状态 |
|---|---|---|
| 未知异常熔断、已知可恢复局部反馈 | 泛型 InvalidOperationException 移出白名单；套件 `global exception policy classifies and fuses` PASS | ✅ 通过 |
| 熔断后拒绝新任务（入口+热键+排队回调） | RuntimeGate.NewWorkAllowed（`App.xaml.cs:849` 热键双保险；Translate 入口拒绝）；套件 `coordinator refuses new work while the fuse is closed` PASS | ✅ 通过 |
| 一次通知、无通知风暴 | 同进程崩溃托盘提示最多一次（2026-09-13 真实故障修复，隔离 `--settings` 实测 0 crash log） | ✅ 通过（含一次真实故障实证） |
| 退出不卡死 | 查词 ForceClose 修复 Shutdown 挂起；重启先清旧资源（热键/窗口/TTS）后交互斥体（V05 四轮收敛，pure 17/17） | ✅ 通过（隔离级） |
| 真机注入演练 + 真实重启交接 | **未验证（E3）** | ⚠️ 验证债 D2 |

### C07 开机启动状态模型（P0/P1）

| 条款 | 证据 | 状态 |
|---|---|---|
| 普通保存绝不覆盖 OS 禁用（F11/V02） | PlanSaveAction：OsDisabled==true 一律 None（`StartupRegistration.cs:195`）；保存走 ExecuteSaveAction、TrySet(true) 仅显式修复入口（W18 复验接线）；pure `startup state machine never overrides an OS disable` + 套件 PASS | ✅ 通过 |
| 命令/路径合同（--background、空格/CJK/带参路径） | BuildRunCommand/ExtractExecutablePath（`StartupRegistration.cs:171-189`）源码核实 | ✅ 通过 |
| 诚实状态（期望/实际/OsDisabled 三态/回滚失败明示） | ReadOsDisabled 三态（读失败=未知≠未禁用）；V03 四轮收敛：基线取磁盘真相 + `_startupRetryPending` 重试锁（`SettingsWindow.xaml.cs:54,222,623-626` 源码核实）；套件 `startup save failure keeps the retry entry and disk truth` PASS | ✅ 通过 |
| marker 在托盘就绪后消费 | `App.xaml.cs:247-250`：CreateTrayIcon() 之后 AnnounceBackgroundStartupFailureIfAny()（源码核实） | ✅ 通过 |
| 8 状态自动化（缺 Run/旧路径/禁用/无权限/移动/换目录/重复启动/崩溃恢复） | pure 决策+命令矩阵全过（含 EnsureRegistered 修复陈旧路径而不清禁用的断言）；注册表经适配器/隔离根 | ✅ 通过（seam 级） |
| 真机临时账户登录 20 次 20/20 到托盘 | **未验证（E3）** | ⚠️ 验证债 D3 |

## 4. 验证债清单（升级后仍开口的子项）

> 规则：债项不阻塞 C00–C07 任务闭环判定，但**全部阻塞 C26 商业发布门**及对应"通过"宣称（§6.5：环境不具备保持"未验证"）。逐项需明确授权/环境后执行。

| 债项 ID | 归属 | 内容 | 证据等级缺口 | 建议触发时机 |
|---|---|---|---|---|
| D1 | C05 | E3 真机失焦矩阵：IME 组合键 Esc、Alt+Tab、系统通知、其他应用失焦（同进程菜单/下拉已隔离级覆盖） | E3 | 真机环境可用时；与 C26 真机矩阵合并执行 |
| D2 | C06 | E3 注入式熔断演练 + 真实重启交接演练（V05 真机：就绪握手/迟到进程/清理失败） | E3 | 获准测试实例后 |
| D3 | C07 | 真机临时账户登录 20 次 20/20 到托盘可用 | E3 | 临时 Windows 测试账户/VM |
| D4 | C04 | E3 跨应用复制粘贴保真（剪贴板真实往返逐字符） | E3 | 真机矩阵时顺带 |
| D5 | C02 | 隔离副本创建失败分支无确定性测试（防御性代码，源码审阅覆盖） | E1→E2 | 可注入文件系统故障的环境 |
| D6 | C03 | 真实崩溃信封在新构建未复验（20260913 crash log 属旧构建）；未来日志导出功能上线时必须接 SanitizeForExport 强制复扫 | E3 + 未来义务 | 下次真机崩溃分析 / 导出功能立项时 |
| D7 | 全局 | HEAD 9e4832a 最终状态的全量套件运行**无原始日志留档**（185/185 仅台账记录；bin/Release 22:14 构建与 22:13 源码对应但未存 log） | A12 构建身份 | **签字前建议**：HEAD 干净树上复跑一轮 LogicTests+PureTests 存档（约数分钟，最稳闭环） |
| D8 | 全局 | 第一轮 E2 生产 DLL 独立探针（F01–F06 原始载体）未在当前构建重跑；当前验证为 pure/LogicTests 直引生产代码（隔离 WPF 宿主编译自生产项目） | E2 | C26 发布门前 |
| D9 | C01→C23 | 真实网络 E2E 授权行为（真实供应商流式主旅程、429/重定向/断网矩阵） | E3 | C23 执行时（真实调用需单独授权与非生产 Key） |

边界说明（非债，防误读）：
- N01 多会话仓（5 会话/2MiB/30 分钟）未实施，是独立任务（挂 W1+），C05 升级不含 N01。
- C09 真实测量运行（30 次启动 P50/P95、内存/CPU 采样）未执行；测量合同/夹具经 22 断言隔离验证——C09 本身仍按台账 §2 独立推进，不随本批升级。
- W1–W18 UI 波次已发生视觉大改（§7 阶段 A"未全部 DONE_VERIFIED 不得视觉大改"的字面约束被 lead 后续授权覆盖，POST §4.1 允许并行）——本对账仅记录该事实，不追溯。

## 5. 结论与建议动作

1. **8/8 判定为 DONE_VERIFIED 候选**：C00–C04 无保留；C05/C06/C07 附条件（E3 子条款按 §6.5 保持"未验证"并挂 D1–D3 债项，不影响任务闭环，但不得用于发布宣称）。
2. 升级执行顺序建议：先完成 D7（HEAD 复跑一轮留档，闭合 A12 构建身份）→ lead 签字 → 台账 §2 行刷新（状态 + 证据列去掉过时的"待实例退出"，引用 3×180/0、185/185、pure 18/18、HEAD 9e4832a 身份与本报告路径）。
3. 验证债 D1–D9 建议转入"验证待办"账本（POST §4.1 第二本账），与 C26 发布门联动排期；D7 是唯一建议在签字前完成的项。
4. 本报告未修改任何产品代码/测试/台账；判定基于 2026-09-14 快照（HEAD 9e4832a，工作树干净）。

## 6. 2026-09-15 后续事实更新（不改动上文判定）

> 本节为事后追加，仅记录本报告落盘之后的新事实；§1–§5 保持 2026-09-14 时点原貌。

- **套件规模继续演进**（§1 表内 180×3、185/185 为当时证据，不是当前数字）：2026-09-15 在当前工作树直接实跑，**LogicTests 203/203、PureTests 23/23 全绿**（含 `real user config unchanged by the run` 与 `no unsanctioned public network send was attempted` PASS），Rust workspace 208/208。数字随新增测试持续变化，以 `scripts/verify.ps1` 实跑为准。
- **验证债 D7 的前提已被超越**：工作树已从 HEAD 9e4832a 继续推进（W21/W30、Prompt 0.1.6 候选等波次），「在 HEAD 干净树复跑一轮」不再是当前构建身份的有效闭环描述；当前证据以最新工作树的全量实跑与台账追加记录为准，D7 不再以原形式执行。
- **D1–D6、D8、D9 维持开口**：E3 子条款与发布门前义务未变；E3 矩阵与真实网络 E2E 仍需环境与授权。
- `scripts/verify.ps1` 与 CI 均已覆盖 PureTests（此前只跑 LogicTests 的缺口已闭合），pure 宿主可在任何实例状态下直接运行。
