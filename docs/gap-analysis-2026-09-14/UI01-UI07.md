# 对账G：UI01–UI07 规划 vs 现实 + 审计索引勘误

> **编制日期**：2026-09-14
> **编制人**：Pi（Teammate / 对账G）
> **任务**：以 V2 `PRODUCT-EXPANSION-REVIEW-V2.md` §14 的 UI01–UI07 原始合同逐条对照代码现实判定 DONE/PARTIAL/NOT_STARTED；同时核对 `docs/ui-audit-2026-09-13/README.md` 的映射表/波次台账/Backlog 与代码的一致性（勘误只写在本报告，不改索引原文）。
> **方法**：只读分析。证据基线：Git HEAD `9e4832a`（2026-09-13 "release: prepare PopGlot 0.1.5"，含 W1–W18 全部波次产物），工作树干净；全部结论以当前代码为准，逐条给出 file:line。
> **关联合同**：V2 §10（配色复审/调色盘合同）、§11（排版尺寸与响应规则）、§12（逐页面目标结构）、§13（控件状态合同）、§15（视觉与可用性验收门槛）。

---

## 一、总判定表

| 任务 | 范围（V2 §14） | 判定 | 主要覆盖波次 | 核心缺口 |
|---|---|:---:|---|---|
| **UI01** | U01/U02/U09/U10 设计基线 | **PARTIAL** | W2/W5/W6/W7/W12/W13 | U01/U02 已修；U09/U10 文档侧未兑现（DESIGN_SYSTEM.md 仍 px 口径、间距网格与代码不一致）；无截图清单；Spacing Token 为死资源 |
| **UI02** | 配色与状态板 | **PARTIAL** | W1/W4/W6 | 深浅两套 token-背景组合已有自动化对比度审计（优于静态 HEX 表）；高对比仅基础版（状态色/选中色未覆盖）；无逐控件可视化状态板 |
| **UI03** | 主窗/浮窗/查词布局 | **PARTIAL** | W3/W7/W11/W12 | 宽度三断点 DONE；低高度方案仅隐式存在未成方案；浮窗语言行仍在标题栏（U04 部分兑现，无"更多"收纳） |
| **UI04** | 设置/服务/隐私 | **DONE（代码层）** | W2/W5/W9/W13/W16 | 四项交付物（表单布局/保存条/错误位置/实际与草稿状态样例）均有代码证据；仅缺视觉样例存档（归 UI07） |
| **UI05** | 资料库/OCR/托盘 | **PARTIAL** | W1/W3/W6/W10 | 资料库侧 DONE；截图无"确认模式"（仅拖选即发）；托盘菜单无"暂停"项（N13 未实施）；资料库窄窗前进/返回未做 |
| **UI06** | 主题生命周期/无障碍 | **PARTIAL** | W1/W3/W7/W12 | 旧文档热切主题 DONE（SetResourceReference 全覆盖）；字体放大 200% 未实施；键盘-only/读屏仅有基础设施（AutomationProperties/FocusRing），无旅程级证据 |
| **UI07** | 集成视觉验收 | **NOT_STARTED**（对 W1–W18 而言） | — | 18 个波次零截图证据；仅有波次前的 user-ui-repair 轮遗留截图（56 张，其中 DPI 矩阵属旧版 UI）；§15 门槛（DPI 矩阵/高对比/文本放大/3 人旅程）全部未执行 |

**统计**：DONE 1 项（UI04，代码层）/ PARTIAL 5 项 / NOT_STARTED 1 项（UI07）。

---

## 二、逐项分析

### UI01 设计基线（U01/U02/U09/U10）— PARTIAL

**合同交付物**：token 角色表、DIP 字号/间距表、当前截图清单、源码与文档差异。

| 子项 | 判定 | 证据 |
|---|:---:|---|
| U01 侧栏挤压（原 MinWidth=520、侧栏 168 固定） | **DONE** | `MainWindow.xaml:6` MinWidth=560；`MainWindow.xaml.cs:596-631`：clientWidth<720 折叠侧栏至 48 DIP 图标模式（BrandText/NavGroupHeader 隐藏、Content 置空保留 ToolTip）。W7/WIN-12、GAP-01 |
| U02 设置窗 820×600 过大 | **DONE** | `SettingsWindow.xaml:6` MinWidth=680 MinHeight=480（W2/SS-14）；`SettingsWindow.xaml.cs:104,483-485` <700 DIP 触发 GeneralSection/ProviderSection `SetCompact` 单列收缩（W13/GAP-02）；`ServicesSection.xaml.cs:122` 编辑器 <680 亦收缩 |
| U09 文档 px/DIP 混用、间距散值 | **PARTIAL** | 代码侧已收敛：XAML 字号实测阶梯 11/12/12.5/13/14.5/20（W5 归一，现存离群值仅 11.5×1、14×3）；`Themes/Controls.xaml:24-30` 定义 Spacing4~24 标尺（W6/TR-18）。**但** `docs/DESIGN_SYSTEM.md` 全文仍用 "px"（§2.2 层级矩阵、§5.2 间距网格），且其间距网格（4/6/8/10/12/14/16/18/20/28）与代码 Spacing 标尺（4/8/12/16/20/24）两套口径并存；无统一 DIP 表文档 |
| U10 "azure blue" 注释/60-30-10 表述失实 | **NOT_STARTED** | `ThemeService.cs:241` 注释仍写 "Accent (azure blue)"；`DESIGN_SYSTEM.md:20` 仍以"60%/30%/10%"陈述配色占比 |
| token 角色表 | **PARTIAL** | `DESIGN_SYSTEM.md §1.1` 有暗色全量表+角色说明（且声明"以代码为准"），亮色仅一行指针（指向 ThemeService.LightTokens）；**高对比模式（W6 已实现）完全未入文档** |
| 截图清单 | **NOT_STARTED** | 无"当前截图清单"交付物。`artifacts/screenshots/`（56 张）系 2026-09-13 user-ui-repair 轮（W1–W18 之前）的遗留产物 |
| 源码与文档差异 | **PARTIAL** | 四份审计报告做过差异核对；但 DESIGN_SYSTEM.md 本身从未与波次产物对账（px 口径、间距网格、高对比缺失即为差异残留） |

**关键发现（波及 TR-18/W13 台账可信度）**：Spacing Token 是**死资源**——全库（含 Controls.xaml 模板自身）零引用：

```
$ grep -rn "StaticResource Spacing" --include="*.xaml" apps/ | wc -l   → 0
$ grep -n "Spacing" Themes/Controls.xaml                              → 仅 24-30 行定义
```

README §三 W6 行与 §五 Backlog 第 2 条均称"并在（核心）模板内（就近）应用"；EXECUTION-LEDGER W13 条目（244-250 行）更声称 `ServicesSection.xaml` 已引入 `{StaticResource Spacing16}`（EditorSectionCard）与 `{StaticResource Spacing12}`。**当前代码中 `EditorSectionCard` 的 Padding 是裸值 `16`（`ServicesSection.xaml:52`），全文件无任何 Spacing 引用**；HEAD 提交 `9e4832a` 内亦无（`git show 9e4832a:...ServicesSection.xaml | grep Spacing` 为空）。结论：台账"已应用"声称与代码不符（要么当时未真正落地，要么后续被还原且未记录）。

### UI02 配色与控件状态板 — PARTIAL

**合同交付物**：深浅/高对比的控件状态板，记录最终颜色与实际背景，"不仅列 HEX"。

- **深浅两套：以自动化审计替代静态状态板（实质达标）**。`tests/PopGlot.Windows.LogicTests/ThemeAuditHelper.cs:42-90` 对 Dark/Light 两套调色盘强制：正文三级文本对 Surface/Input/ResultSurface ≥4.5:1；BorderStrong 输入/表面边界 ≥3:1；状态徽章对软衬底 ≥4.5:1；PrimaryText 对 Primary/PrimaryHover/PrimaryPressed ≥4.5:1（即 **hover/pressed 组合态的最终合成色已被机器校验**）；Danger hover/pressed 换深底红字的组合态亦覆盖；FocusBrush 对 Canvas/Surface ≥3:1；输入面与结果面亮度差 ≤2%。这比一张手工状态板更强。
- **模板状态补齐**：W1/W4 补齐 IsPressed（TR-14）、输入框 hover 边框（TR-06）、禁用态单层淡化（TR-04，移除 Surface.Opacity 叠加）；W7 补浮窗 Pin/Star 按下/禁用态与 FocusVisualStyle（`TranslationPanelWindow.xaml:52-88`）。
- **高对比：基础版，未全覆盖**。`ThemeService.cs:50-127`：检测 `SystemParameters.HighContrast`，将 Canvas/Sidebar/Surface/SurfaceMuted/SurfaceRaised/Input/ResultSurface → Window 色，三线文本 → WindowText/GrayText，Accent/Focus/Primary → Highlight，边框 → WindowText，阴影透明；`EnsureSystemWatcher`（180-206 行）订阅 UserPreferenceChanged（含 Accessibility 类别）热重应用。**缺口**：Danger/Success/Warning 及其 Soft 衬底、AccentSoft、文本选中前景/背景未做系统色覆盖（高对比下仍用品牌软衬底色）；§10.3 要求补足的"文本选择前景/背景、当前导航背景、代码背景/边界、错误说明、待保存提示、链接、禁用边界、阴影"角色中，选中色/导航选中背景等仍缺独立 token；高对比模式无任何自动化或视觉验证。
- **可视化状态板交付物本身**：不存在（无逐控件 default/hover/pressed/focus/selected/disabled 的图板或最终合成色表）。

### UI03 主窗/浮窗/查词布局 — PARTIAL

**合同交付物**：每窗宽/窄/低高度方案，语言行与主动作层级；关联 N01/N04/N14。

| 子项 | 判定 | 证据 |
|---|:---:|---|
| 主窗宽度三断点 | **DONE** | `MainWindow.xaml.cs:588-602`：统一按客户区宽度（扣除窗框口径，G07）——<720 折叠侧栏+堆叠；720–959 标准 168 DIP 侧栏；≥960 `SetWide` 宽屏增强（W7 WIN-12/GAP-01 + W12） |
| 浮窗宽度方案 | **DONE** | `TranslationPanelWindow.xaml:4` MinWidth=460；W3 锚点锁定防翻转（WIN-04）；W11 首帧瘦身（PERF-HOTKEY-02/03） |
| 查词窗口 | **DONE** | `QuickSearchWindow.xaml.cs:76-110`：光标所在屏工作区居中 + MaxHeight 钳制不溢屏（W7 WIN-10）；`SizeToContent="Height"` 空态收缩、结果出现再扩展（符合 §12 极速查词行）；150ms 防抖（W11） |
| 低高度方案 | **PARTIAL** | 仅隐式存在：设置窗 MinHeight=480 + 纵向滚动（W2）、主窗 MinHeight=560、面板 MinHeight=300 且输入区 MaxHeight=86/结果 MinHeight=120（`TranslationPanelWindow.xaml:170-215`）。§11.2"低高度优先保留标题/关闭/主动作/内容滚动"未逐窗设计或验证 |
| 浮窗标题栏层级（U04） | **PARTIAL** | 尺寸/间距已达标：按钮全 32×32、关闭前 8 DIP 间隔（`TranslationPanelWindow.xaml:138-146`，W3/W7 WIN-05/17）；PinToggle 动态 ToolTip（W12 WIN-19，`TranslationPanelWindow.xaml.cs:1509-1513`）。**但**语言行（源/交换/目标下拉）仍在标题栏中列（`TranslationPanelWindow.xaml:93-110`），未按 §12"语言放可响应工具行；低频动作收进更多"重组——展开/设置两按钮未收纳，无溢出菜单 |
| 语言行与主动作层级（主窗） | **DONE** | `TranslateSection.xaml:27`（PrimaryButton 唯一主按钮）、67-101/265-281（输入/结果各配语言栏）；U11 引擎状态收进底栏可操作摘要（`MainWindow.xaml:127-146` EngineHealthButton，长名省略+ToolTip） |

### UI04 设置/服务/隐私 — DONE（代码层）

**合同交付物**：表单布局、保存条、错误位置、实际状态及草稿状态样例。

- **表单布局**：设置行两列（标签列+控件列），窄窗 <700 单列收缩（`SettingsWindow.xaml.cs:483-485` + 两分区 `SetCompact`，W13）；服务编辑器 <680 亦收缩（`ServicesSection.xaml.cs:106,122`）。
- **保存条**：编辑会话期间收拢窗体底栏（W5/SS-12）；DraftGuardBar 展开时 EditorActionBar 置 Collapsed，杜绝同屏双保存（W5/SS-13）；唯一主动作原则（W2/SS-03 验证连接降级、U03 编辑上下文只突出保存——`ServicesSection.xaml` Verify/TestStatus 为次要样式）。
- **错误位置**：字段侧 TestStatusPanel/TestDetailText（`ServicesSection.xaml:332-336`）；错误卡带"打开设置"修复路径（`QuickSearchWindow.xaml:172-191`、面板 RenderFailure W7/WIN-08）；保存失败保草稿、失败如实回显（C07 语义，W16 收藏失败可见性返修加固）。
- **实际状态与草稿状态样例**：开机启动"实际状态+修复动作"行（`GeneralSection.xaml:44-66` StartupStateHint + StartupRepairButton，W2/SS-11 + W9 异步化）；高级折叠（`ServicesSection.xaml:428` AdvancedExpander）；删除隔离（`:497` DeleteServiceButton 左对齐 DangerButton，远离保存）；隐私分区按图片/文字/云朗读分别说明+纯离线模式+Expander（`PrivacySection.xaml:27,67,79,98,130`）。
- **保留缺口**：§15 要求的 dirty/saving/save-failed 全状态矩阵无视觉证据（属 UI07 范畴）；快照基线与保存条耦合处 W5 采用最小实现（编辑器打开期间隐藏）的取舍见 W5 台账。

### UI05 资料库/OCR/托盘 — PARTIAL

| 子项 | 判定 | 证据 |
|---|:---:|---|
| 资料库搜索空态/无匹配区分 | **DONE** | W1/TR-11（有文本 0 匹配不再误报"还是空的"，`LibrarySection.xaml.cs:310` 区域）；损坏只读不伪装空列表（W1/TR-03，`:188` 损坏隔离+重试加载）；清空确认显示真实计数（W6） |
| 内容详情/分栏/删除邻近选中 | **DONE** | W6/TR-10 GridSplitter + 删除后 Math.Min(index,count-1) 邻近选中；W10 虚拟化+ICollectionView.Filter 就地过滤（PERF-LIST-02） |
| 窄窗列表/详情"前进/返回" | **NOT_STARTED** | `LibrarySection` 无任何窄窗 SizeChanged/导航栈逻辑（grep 无 narrow/SetCompact）；仅靠 GridSplitter Min=220 撑窄窗 |
| 截图四角假手柄（U/WIN-09） | **DONE** | 假手柄已移除；现仅 SelectionBorder 选框 + SizeBadge 尺寸标记 + HintChip（`CaptureOverlayWindow.xaml:36-60`）。注：`CaptureOverlayWindow.xaml:15` 注释仍提"four corner handles"，属陈旧注释 |
| 确认捕获（快速/确认两模式） | **NOT_STARTED** | 单一"拖选即发"模式（MouseUp 即 `CaptureAndCloseAsync`，`CaptureOverlayWindow.xaml.cs:113-114`）；无提交前确认/调整模式；可拖拽改选区不存在（合同"可拖控制点仅在真可拖时出现"的前半句无从谈起） |
| 托盘菜单合同 | **PARTIAL** | `App.xaml.cs:1201-1246`：打开→极速查词→恢复最近翻译→分隔→翻译选中/截图翻译/OCR（动态显示当前快捷键，Opening 时刷新）→分隔→设置→退出。结构符合"打开/恢复→翻译入口→设置→分隔→退出"骨架；WinForms 系统渲染（浅深/系统可读性 OK，无固定轮询）。**缺**：合同明列的"暂停"项（N13 快捷键暂停未实施）；菜单名不含运行状态（如热键已暂停态） |

### UI06 主题生命周期/无障碍 — PARTIAL

**合同交付物**：同一窗口旧文档换主题、字体放大、键盘-only 和读屏证据。

| 子项 | 判定 | 证据 |
|---|:---:|---|
| 旧文档热切主题（U06） | **DONE** | `Services/MarkdownPresenter.cs` 全链 SetResourceReference（:289 文档前景、:393/406 列表符号、:494-534 行内代码/token、:585-586/646 代码块背景边框——普通字/代码/徽章全部动态，W1/TR-02）；从属窗口全量订阅 ThemeChanged（`QuickSearchWindow.xaml.cs:54`、`TranslationPanelWindow.xaml.cs:113`、`FloatingTriggerWindow.xaml.cs:42`、SettingsWindow/MainWindow 同，W3/W7/TR-08）；W4/TR-09 清除 FindResource 静态赋值 |
| 字体放大 200% | **NOT_STARTED** | 全库无字号设置/缩放因子（grep 字号/FontScale/放大 零命中）；§11.1"翻译正文可调字号"与 §15"文本放大 200% 单独验证"均未实施（DPI 缩放≠文本放大，合同明示不可互替） |
| 键盘-only | **PARTIAL** | 基础设施在：FocusRing 样式（`Themes/Controls.xaml:72`）+ 各核心样式 FocusVisualStyle（:350,491,563,608,673,1054,1171…）；快捷键（Esc 关闭/Ctrl+R 重试/Ctrl+S 收藏/Ctrl+C 复制/Ctrl+P 朗读）。**缺**：§15.5"整条旅程键盘可完成、焦点不丢失不被遮挡"的旅程级验证证据（无测试、无走查记录） |
| 读屏证据 | **PARTIAL** | AutomationProperties.Name 广泛使用（11 个 XAML 文件命中）；状态区 LiveSetting="Polite"（`MainWindow.xaml:136-137`、`QuickSearchWindow.xaml` FooterStatus）。**缺**：真实读屏（讲述者/NVDA）运行证据 |

### UI07 集成视觉验收 — NOT_STARTED（对 W1–W18 波次）

**合同交付物**：前后对应图、差异说明、残留问题、逐条结论；"不接受只有新 golden 图"。

- **现状**：W1–W18 全部波次以"静态审查 + 三项目 Release 编译 0 警告 0 错误 + LogicTests/PureTests"验收（各波次台账均可查），**无一张波次后截图**。`artifacts/screenshots/`（56 张，文件时间 2026-09-13 22:12，早于审计索引 README 成文）属 user-ui-repair 轮（HEAD 21f0132 时代，W1 之前）的旧 UI 证据，不能代表 18 波次后的现状。
- 该轮验收文档 `RESULT-2026-09-13-user-ui-repair.md:41` 自认 **T7 布局/多 DPI/主题矩阵"未验证（本轮未跑 150%/175%/High Contrast 截图矩阵）"**——而 W6/W7/W12/W13 恰恰改动了高对比、多屏 DPI、断点、单列收缩这些 T7 未验证的领域。
- §15 门槛逐项：①浅/深主题正常+关键失败画面截图——无；②125/150/200% DPI+文本放大 200%——无（仅旧轮 library/main 的 DPI 矩阵）；③同窗深→浅→系统往返——无运行证据（仅有代码层动态绑定保证）；④高对比/减少动画——无；⑤键盘整旅程——无；⑥U01–U12 逐条通过/不通过/未验证表——无；⑦3 位试用者旅程——无。
- **判定依据**：UI07 的本质是"证据交付物"而非代码改动，18 个波次没有产出任何一层该证据 → NOT_STARTED（注：若把旧轮 56 张截图与 RESULT 文档算作部分基础，也仅覆盖 W1 之前的 UI 状态，对本轮合同不构成兑现）。

---

## 三、审计索引勘误清单（docs/ui-audit-2026-09-13/README.md）

以下勘误**只记录于本报告，未改动索引原文**。按严重度排序：

| # | 位置 | 索引声称 | 代码现实（证据） | 建议 |
|---|---|---|---|---|
| 1 | §二 TR-18 行、§三 W6 行、§五 Backlog 第 2 条 | Spacing 标尺"并在模板内部就近应用/在核心模板内应用" | **死资源**：全库零 `{StaticResource Spacing*}` 引用；`Themes/Controls.xaml:24-30` 仅定义 | 改为"已建立定义，模板/页面接入未落地（见对账G）"或真正接入后再标已应用 |
| 2 | EXECUTION-LEDGER W13 条目（:250） | `ServicesSection.xaml` 已引入 `{StaticResource Spacing16}`（EditorSectionCard）与 `{StaticResource Spacing12}` | `ServicesSection.xaml:52` 为裸值 `16`；全文件无 Spacing 引用；HEAD `9e4832a` 内同样没有 | 台账虚标（跨文档发现，一并勘误）：W13-2 应改"未落地/已回退" |
| 3 | §二 GAP-02 行、§五 Backlog 第 1 条 | "未排期"；Backlog 称"后续规划：<560 DIP 折行" | **W13 已实现**：`SettingsWindow.xaml.cs:104,483-485` <700 DIP → SetCompact（GeneralSection/ServicesSection 均有 SetCompact） | GAP-02 改"已修复+lead复核（W13）"；Backlog 第 1 条整条移除或改历史备注（注意阈值口径 700 不是 560） |
| 4 | §二 WIN-19 行 | "未排期" | **W12 已实现**：`TranslationPanelWindow.xaml.cs:1509-1513` PinToggle_Changed 动态 ToolTip/状态文案 | 改"已修复+lead复核（W12）" |
| 5 | §二 PERF-IO-03/04/05 行、§三 W9 行 | "进行中" | **W9 已完成并复核**（看板已完成；W16/W18 返修进一步触及 StartupRegistration/SettingsWindow/DiagnosticsLog）；代码中注册表/Profile/日志均已异步化 | 三行改"已修复+lead复核（W9）"，W9 行改"已完成" |
| 6 | §三 波次台账 | 表列 W1-W12、W14、W15；标题"W1 ~ W15" | **缺 W13 行**；**缺 W16/W17/W18 三个紧急返修波次**（看板均已完成：写盘失败可见性/面板四项回归/授权合同与线程加固） | 补 W13、W16、W17、W18 四行，标题改"W1 ~ W18" |
| 7 | §三 W15 行 | 状态"部分返修完成" | 看板 W15 已完成；预热撤回与 PERF-HOTKEY-01"方案退回"表述与代码一致（`App.xaml.cs` 无 Prewarm，grep 零命中） | 状态行可改"已完成（预热方案撤回，见 PERF-HOTKEY-01）" |
| 8 | §二 PERF-IO-06 行 | "已修复（W15）" | **属实**：`App.xaml.cs:458` RestartApplication 为 async + `Task.Run(() => RestartHandover.Run(...))`，WaitForExit(5000)（:643）在后台委托内执行 | 无需勘误（确认项） |

另有一处代码内陈旧注释（非索引）：`CaptureOverlayWindow.xaml:15` 注释仍描述"four corner handles"，而假手柄已在 W3/WIN-09 移除——下次触碰该文件时顺手更正即可。

---

## 四、建议下一步（3 条）

1. **补一轮轻量视觉验收收口 UI07（最高优先）**：复用 user-ui-repair 轮的隔离数据根+截图流程（`RESULT-2026-09-13-user-ui-repair.md` 的命令模板），针对 18 波次实际改动的面拍浅/深两套截图——主窗三断点（<720/720-959/≥960）、浮窗正常+失败态、查词空态+错误卡、设置窄窗单列、资料库空态/无匹配、高对比模式；并对 library/main 复跑 125/150/200% 矩阵补上 T7 未验证项。产出前后对照+残留清单即满足 UI07"不接受只有新 golden 图"的合同。
2. **文档—代码对账修正收口 UI01**：DESIGN_SYSTEM.md 全文 px→DIP、间距网格与 Spacing 标尺统一口径、补高对比一节（引用 `ThemeService.ApplyHighContrastOverrides`）、修正 60/30/10 与 "azure blue" 表述（`ThemeService.cs:241`）；同步按上表 1-7 项勘误 README 索引与台账虚标。
3. **对 Spacing 死 Token 做去留决策**：要么真正接入（Controls.xaml 模板 + 业务页面按 W13 原方案），要么从 `Controls.xaml:24-30` 移除并在台账注明撤回——保留死资源会让后续审计继续误判"标尺已应用"。
