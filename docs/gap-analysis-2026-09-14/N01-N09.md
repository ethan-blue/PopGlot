# 对账E：N01–N09 功能规划 vs 现实

- 日期：2026-09-14
- 执行者：Pi（对账E）
- 合同依据：`docs/review-2026-09-12/PRODUCT-EXPANSION-REVIEW-V2.md` §5（N01–N09 条款，状态均标 `SPECIFIED_NOT_IMPLEMENTED`）
- 现实依据：当前工作树代码（`apps/PopGlot.Windows`、`crates/popglot-core`）+ `docs/review-2026-09-12/EXECUTION-LEDGER.md` 追加记录 + `docs/ui-audit-2026-09-13/*` 波次映射
- 方法：逐项读 V2 §5 原始合同 → 对照代码现实（file:line）→ 判定 DONE / PARTIAL / NOT_STARTED；PARTIAL 列缺失子项
- 通用规则遵守：只读分析，未改任何产品代码/测试/台账/索引；未跑 GUI；未 git 操作

## 1. 判定统计

| 判定 | 项 |
|---|---|
| DONE | 无 |
| PARTIAL | N01、N02、N04、N05、N07、N09（6 项） |
| NOT_STARTED | N03、N06、N08（3 项） |

> 说明：台账 §2 对 N01–N09 仍全部标 `SPECIFIED_NOT_IMPLEMENTED`。本对账结论是"该标注已滞后于代码现实"：C00–C07 批次（C02/C04/C05/A05/A06 等）与 2026-09-13 UI 波次（W1–W18）在未领取 N 任务的情况下实质性落地了 N01/N02/N04/N05/N07/N09 的部分子项。但没有任何一项达到 DONE。

## 2. 逐项判定表

### N01 会话暂存、误关恢复与工作台接续【L1】 — **PARTIAL**

C05/A05/A06 批次已落地"单会话隐藏-恢复"骨架；V2 点名的多会话暂存仓从未实现（台账 149 行 A06 明记"N01 的 5 会话/2MiB/30 分钟多会话仓未在本批实现（挂 W1，未冒充完成）"，而 W1 实际做了主题层，台账 618 行）。

| 子项 | 状态 | 证据 |
|---|---|---|
| 托盘"恢复最近翻译" | ✅ | `App.xaml.cs:1215` + `RestoreRecentSurface`（`App.xaml.cs:1098–1130`），按 `_panelLastUsedUtc/_quickSearchLastUsedUtc` 显式元数据择近恢复（A06） |
| 可选独立恢复快捷键 | ❌ | `HotkeyAction` 仅 4 项（TranslateSelection/CaptureScreen/ClosePanel/ShowWindow，`ShellSettings.cs:9–15`） |
| 自动隐藏只改可见性；请求跑满 deadline；隐藏期不自动复制/朗读/抢焦点 | ✅ | `App.xaml.cs:1064–1073`（CloseActivePanel 注释 C05）；`TranslationPanelStreamGate.cs:151–158`（WindowVisible + ShouldTriggerAutoCopy，A05） |
| 显式 X/关闭快捷键取消在途请求后隐藏；取消不写自动历史 | ✅ | `TranslationPanelWindow.xaml.cs:1586–1593`（CloseAsUserIntent，A05）；`TranslationCoordinator.cs:1214–1221`（IsCleanCompletion 门禁，取消/失败永不入历史） |
| 多会话仓：≤5 会话 / 累计 ≤2MiB / 隐藏 30 分钟淘汰 / 超限显示原因 | ❌ | 全库无实现；现状仅单 `_activePanel` + 单 `_activeQuickSearch`（`App.xaml.cs:48` 等） |
| 图像不进隐藏会话仓；任务完成/取消后释放 | ⚠️ 部分 | `OnClosed` 释放 `_screenshot`（`TranslationPanelWindow.xaml.cs:1595–1607`）；但隐藏（Hide 不触发 OnClosed）后面板仍持有 `_screenshot` 供重试至被替换——单会话模型下的偏差 |
| 展开到工作台携带会话与结果、不二次请求 | ✅ | `TranslationPanelWindow.xaml.cs:1025–1034`（ExpandButton_Click → `_openInMain`）+ `TranslateSection.FocusTranslate`（`Sections/TranslateSection.xaml.cs:1108–1143`，状态"已从浮窗展开，未重新翻译"） |
| 工作台有未提交草稿时先给保留/替换选择 | ❌ | `FocusTranslate` 无条件覆盖 `TranslateInput.Text`（`Sections/TranslateSection.xaml.cs:1114–1117`） |
| 退出清除内存暂存；跨重启恢复默认关闭 | ✅（平凡） | 进程退出即清空；无跨重启恢复功能（等于默认关闭） |

### N02 输入、IME、快捷键和来源复制【L1】 — **PARTIAL**

合成输入链路的健壮性（修饰键/UIPI/剪贴板快照）已由早期批次做实；但 V2 §3 点名的 IME 组词判定语义错误至今未修，去重与前台核验子项未按合同实现。

| 子项 | 状态 | 证据 |
|---|---|---|
| IME 组词中 Enter 仅确认候选、不提交 | ❌（仍是缺陷） | 三入口均保留 V2 §3 点名的错误谓词 `InputMethod.GetIsInputMethodEnabled`（IME 启用≠正在组词；中文 IME 开启时 Enter 永不触发翻译）：`TranslationPanelWindow.xaml.cs:1210`、`QuickSearchWindow.xaml.cs:182`、`Sections/TranslateSection.xaml.cs:529` |
| 默认 Enter 翻译、Shift+Enter 换行 + 三入口显示提示 | ✅ | 同上三处 KeyDown；提示：面板占位符 `TranslationPanelWindow.xaml.cs:426`、主窗 `TranslateSection.xaml.cs:390,395`；极速查词占位符无键位提示（`QuickSearchWindow.xaml:46`，次要缺口） |
| "Enter 换行、Ctrl+Enter 翻译"偏好 | ❌ | 无设置项 |
| Ctrl+C 有选区复制选区；无选区不劫持；译文用按钮/专用组合 | ✅ | 极速查词 Ctrl+C 保持编辑器原生行为、Ctrl+Shift+C 专用复制译文（`QuickSearchWindow.xaml.cs:697–710`）；各窗复制按钮 |
| 清空输入支持本地撤销、不连带删历史 | ⚠️ 部分 | `SourceClear_Click`（`TranslationPanelWindow.xaml.cs:1042–1059`）清空但不删历史 ✅；无撤销 ❌ |
| 连续相同内容提交在同一活动请求中去重 | ❌ | 无去重逻辑（重复 Enter 会取消旧请求再发新请求） |
| 修改后提交取消旧请求、旧回调无效 | ✅ | epoch + CTS：`TranslateSection` `_currentEpoch`、`QuickSearchWindow.xaml.cs:132–169`（W11 防抖+版本号丢弃过期结果）、面板 `_operation` |
| 划词复制前核验前台；用户切窗则停止并提示"目标窗口已改变" | ❌（实现方向相反） | `ClipboardSelectionService.cs:154–161`：前台变化时 `SetForegroundWindow(TargetWindow)` 把目标窗口拉回——既是强抢焦点（合同明令禁止）也无"请重试"提示 |
| 修饰键长按有界处理 | ✅ | `ClipboardSelectionService.cs:164–215`（500ms deadline 等待 + 合成 KeyUp 净化修饰键） |
| UIPI 有界失败 + 手动粘贴路径 | ✅ | `ClipboardSelectionService.cs:204`（管理员隔离明确报错并建议手动复制粘贴） |
| 剪贴板快照恢复不覆盖用户数据 | ✅ | `ClipboardSelectionService.cs:89–94, 243–251, 467–494` |

### N03 截图调整、OCR 校对与明确上传【L1 优化 / L2 校对】 — **NOT_STARTED**

N03 合同的全部增量（确认模式、预览冻结、OCR 校对、断行局部清理、上传明示、最近选区失效）均为零实现。存量能力（快速框选、Esc/右键取消、OCR 安装指引、OCR 失败不自动转上传）是 C 批既有，且 <6px 误点直接 Close() 恰是 N03 要改掉的点名行为。

| 子项 | 状态 | 证据 |
|---|---|---|
| 默认快速框选路径保留 | ✅（存量） | `CaptureOverlayWindow.xaml.cs:90–114`（松开即捕获）、Esc/右键取消（117–132） |
| "框选后确认"模式（拖动选区/四角/Enter 确认/重新框选） | ❌ | 无 |
| <6px 误点回到等待框选而非消失 | ❌ | `CaptureOverlayWindow.xaml.cs:104–108` 直接 `Close()`（V2 §3 点名行为，未改） |
| 预览使用冻结已捕获画面 | ❌ | 无预览界面 |
| OCR 校对界面（原图缩略图/可编辑文字/不重跑 OCR） | ❌ | 无；截图翻译为 OCR→翻译直通（`TranslationPanelWindow.xaml.cs:228–256`） |
| 断行清理/连字符移除仅作用选择范围且可撤销 | ⚠️ 偏差 | 既有 `MergeLines_Click` 全文一次性合并、无撤销、无作用范围（`TranslationPanelWindow.xaml.cs:1051–1067`）——属 C 批存量，非 N03 合同形态 |
| 上传前明示"只发送校对后文字 / 将发送选区图片" | ❌ | 无 |
| 本地 OCR 不可用给安装指引与替代选择 | ⚠️ 部分 | 安装指引 ✅（`WindowsOcrService.cs:64`，含 Windows 设置路径）；失败直接抛错无"替代选择"入口（`TranslationPanelWindow.xaml.cs:233–236`）；未自动转上传 ✅ |
| 最近选区只记坐标与显示器标识、变化后失效 | ❌ | 无 |

### N04 阅读、复制格式与 partial 使用【L1】 — **PARTIAL**

W3（WIN-02/G06）+ W11 + C04 批次实质兑现了 partial 显式复制、副作用三分类、代码块复制与流式跟随；复制格式菜单与阅读位置保持未做。

| 子项 | 状态 | 证据 |
|---|---|---|
| Partial 显式"复制已生成部分"（明确提示可能缺失） | ✅ | W3 WIN-02：`TranslationPanelStreamGate.cs:125–137`（CanCopyPartial/CanCopy + 警告横幅 `GetPartialWarningBanner`）、`TranslationPanelWindow.xaml.cs:604`（partial 时 ResultCopyBtn.IsEnabled=true）；台账 664 行 |
| 动作资格三类拆分；不放宽 IsCleanCompletion | ✅ | `TranslationPanelStreamGate.cs:139–149`（CanPerformResultActions 仅干净完成；自动复制/TTS/收藏对 partial 禁止）；`TranslationCoordinator.cs:1214`（IsCleanCompletion 未动） |
| 复制菜单"纯文本 / Markdown 原文 / 仅所选内容" | ❌ | 所有入口仅单一"复制译文"（纯文本，`MarkdownPresenter.ToPlainText`）：`TranslationPanelWindow.xaml.cs:972,1148,1168`、`QuickSearchWindow.xaml.cs:540,584`、`TranslateSection.xaml.cs:1086` |
| 译文与解释分别复制 | ❌ | 解释区（ExplanationBox）无复制入口 |
| 代码块复制保留内部内容 | ✅ | `MarkdownPresenter.cs:266–268, 284–296, 630–643`（C04/F06 按偏移量精确复制围栏内字节；partial 时按钮禁用） |
| 复制不含服务信息/水印/警告 | ✅ | 全路径复制前过 `ToPlainText` 净化 |
| 流式保留文本选择；上滚停止自动跟随 | ✅ | stick-to-bottom：`TranslationPanelWindow.xaml.cs:481–495`、`Sections/TranslateSection.xaml.cs:638–646`（`Ui.IsScrolledToBottom`，`Ui.cs:156–159`） |
| "回到最新"按钮 | ❌ | 无 |
| 终态渲染保持阅读位置、不清除正在选择的文本 | ❌ | 终态切换渲染层（流式 TextBox → 富文本 RichTextBox），选中与位置随之丢失，无保持逻辑 |
| 大代码行横向滚动、页面不整体横向溢出 | ⚠️ 偏差 | 代码块 `TextWrapping.Wrap` 折行（`MarkdownPresenter.cs:636–644`）：无横向滚动（合同措辞），但确实不溢出 |

### N05 语言选择、翻译方向与风格【L1 语言 / L2 风格】 — **PARTIAL**

基础语言对/持久化/交换已有；V2 要求的语言搜索-收藏、检测反馈、交换守卫、风格系统全部未做。

| 子项 | 状态 | 证据 |
|---|---|---|
| 语言目录；auto 仅源语言可选 | ✅ | `LanguageCatalog.cs:46–50`（目标语言无 auto） |
| 切语言不自动重译 | ✅ | 仅 `PersistLanguagePair`（`Sections/TranslateSection.xaml.cs:942–946`）；面板截图例外有注释说明（OCR 引擎可变） |
| 语言本地搜索 / 最近 5 项 / 收藏 5 项 | ❌ | 无（ComboBox 全量列表） |
| 自动检测结果不确定显示"未确定"、不反写 | ❌ | `UpdateAutoDetectHint` 仅静态提示控件可见性（`Sections/TranslateSection.xaml.cs:951–955`）；无检测结果反馈链 |
| 源=auto 时交换仅在检测到具体语言后可用并说明原因 | ❌（实现方向不同） | `LanguageCatalog.Swap`（`LanguageCatalog.cs:102–115`）：auto 时猜测反推一对可用语言对，而非禁用+说明 |
| 交换以完整译文为新输入、保留原输入可撤销、不自动发送 | ⚠️ 部分 | 译文入输入框 + 重置状态不重发 ✅（`Sections/TranslateSection.xaml.cs:985–1015`）；原输入直接被覆盖、无撤销 ❌ |
| 风格（忠实/自然/正式）二级菜单、技术保护独立 | ❌ | 全库无风格概念（L2 未启动） |
| "增加解释"独立选项（明示等待/费用） | ❌ | 解释由引擎自动返回，无开关 |
| 风格进请求快照与缓存 key | ❌ | 无（随风格缺失） |
| 应用 UI 语言与翻译语言分开 | ⚠️ 部分 | UI 固定中文（翻译语言独立设置 ✅）；英文界面作为后续任务未做 |

### N06 可编辑生词与真正的术语库【L2】 — **NOT_STARTED**

生词本本体（收藏/删除/导出/只读保护）是 C02/V04 批存量；N06 的编辑能力与术语系统（含 Rust core 侧）均为零。

| 子项 | 状态 | 证据 |
|---|---|---|
| 生词收藏/删除/清空/CSV/MD/Anki 导出/32MiB 保护（存量） | ✅（存量，非 N06 增量） | `Services/VocabularyStore.cs:185–432, 629`；`Sections/LibrarySection.xaml.cs`（W10 虚拟化+差量） |
| 编辑释义/备注/标签/源目标语言 | ❌ | 无 Edit API（VocabularyStore 仅 ToggleStar/Remove/Clear/RetryLoad）；`VocabularyWord.Tags` 字段存在但 UI 从未暴露（`VocabularyStore.cs:9–18`）；无备注字段 |
| "用于术语规则"待确认动作 | ❌ | 无 |
| 术语库（id/大小写/启用/冲突阻止/长词优先/语言对精确匹配） | ❌ | 无任何术语存储 |
| 请求只携带实际匹配术语（≤100 条/≤16KiB）+ "应用 X 条"详情 | ❌ | 无 |
| Rust core 术语协议 | ❌ | `crates/popglot-core/tests/prompt_contract.rs:493–507` 明确断言"Current TranslationRequest deliberately has no fake production glossary field"——core 侧也无术语链 |

### N07 历史检索、结果版本与清理【L2】 — **PARTIAL**

搜索/空态区分/保留限额/清空计数由存量+W1/W6/W10 兑现；筛选维度、服务快照、撤销、批量选择缺失。

| 子项 | 状态 | 证据 |
|---|---|---|
| 历史文本搜索 | ✅ | `Sections/LibrarySection.xaml.cs:72–88`（ICollectionView.Filter，W10） |
| 来源/语言对/日期筛选 + "仅收藏"视图 | ❌ | 仅文本包含匹配，无其他维度 |
| 无结果写"未找到匹配"非"暂无历史" | ✅ | `Sections/LibrarySection.xaml.cs:283–290`（W1） |
| 载入历史只展示旧结果、不重发 | ✅ | `MainWindow.xaml.cs:546–570`（OnLoadToTranslate 填充+badge） |
| 明确当时服务和时间 | ⚠️ 部分 | 列表显示时间（`LibrarySection.xaml.cs:242`）；**TranslationHistoryEntry 无 Service 字段**（`HistoryStore.cs:10–19`），"当时服务"无处可取 |
| "重新翻译"生成关联新会话 | ❌ | 无显式按钮（用户手动改输入重译）；badge 只写"历史记录"来源类型 |
| 历史保持原文/语言快照不随当前设置重标 | ✅ | 载入用条目自带语言对（`LibrarySection.xaml.cs:447–451`） |
| 批量导出/删除显示精确选中数 | ❌ | 导出为全量（`LibrarySection.xaml.cs:481–494`）；无多选批量操作 |
| 单条删除 10 秒仅内存撤销 | ❌ | `DeleteRow` 立即删除+状态提示（`Sections/LibrarySection.xaml.cs:412–440`，W6 只加了邻近选中） |
| "立即清空所有数据"明确提示（真实数量） | ✅ | `Sections/DataSection.xaml.cs:26–30`（W6："将清空 X 条历史记录/Y 个生词"） |
| 历史关闭不新增、已有不删 | ✅ | `_history.TryAdd(entry, HistoryEnabled)`（`TranslationCoordinator.cs:1246–1252`）；清空仅用户显式触发 |
| 保留规则 200 条/90 天不偷偷扩大 | ✅ | `HistoryStore.cs:31–35`（MaxEntries=200/MaxAge=90d）；4MiB 字节限额未见 |
| 重载不跳选/滚动位置保留 | ⚠️ 未验证 | W10 差量更新（Zip 对比 `LibrarySection.xaml.cs:226`）后大体达成；无针对性验证 |

### N08 备份、导入与本机恢复【L2】 — **NOT_STARTED**

全库（含 Rust crates）无任何备份/恢复实现；仅有单向导出（CSV/MD/Anki），非备份格式、无 manifest/校验/事务恢复。合同全部子项（manifest schema、敏感性提示、凭据排除、预检-预览-合并/替换-事务提交、zip 防穿越/限条数/限总量、CSV 公式字符防护）均为零。

| 子项 | 状态 | 证据 |
|---|---|---|
| 设置内"导出数据备份 / 恢复备份" | ❌ | DataSection/LibrarySection 无相关 UI；全库 grep 无 Backup/manifest/恢复备份 |
| 单向导出（存量，非备份） | ⚠️ 存量 | `Sections/LibrarySection.xaml.cs:455–510`（历史 CSV/MD、生词 CSV/MD/Anki） |

### N09 朗读控制与语言真实性【L2】 — **PARTIAL**

授权分离/停止替换/大小上限/临时文件释放是 C23/A10 批存量；播放状态联动由 W5/W7/W14 兑现；暂停/语速/语音列表/选中朗读未做。

| 子项 | 状态 | 证据 |
|---|---|---|
| 停止；新朗读取消旧合成与播放 | ✅ | `TtsService.cs:60–137`（Speak 前置 Stop + generation 计数）、`139–186`（Stop 取消在途云合成） |
| 播放/暂停（支持则显示，不支持不伪装） | ⚠️ 部分 | 仅停止（不伪装暂停 ✅）；MediaPlayer 暂停能力未暴露、无暂停 UI |
| 语速控制 | ❌ | 无 |
| 按语言可用语音列表/选择/安装指引 | ❌ | 自动选语音（`TtsService.cs:225–238`）；**无匹配语音时静默回退 `DefaultVoice`**（:237–238）——违反"不能静默用错误语言" |
| 默认本地语音；云端独立授权 | ✅ | `TtsService.cs:66–78`（CloudSpeechEnabled 独立同意，网络权限升级不隐式授予，`ShellSettings.cs:230`） |
| "朗读选中内容"（选区感知） | ❌ | 仅整段译文/整段原文朗读 |
| 原文独立朗读入口 | ✅ | `TranslationPanelWindow.xaml:189–190`（朗读原文按钮）、`TranslateSection` SourceSpeak |
| 播放中状态联动（图标/ToolTip/可停止） | ✅ | W5 SS-10（`Sections/TranslateSection.xaml.cs:333` 订阅 SpeakingStateChanged；台账 280 行）、W7 WIN-14（`QuickSearchWindow.xaml.cs:617–622`；台账 707 行） |
| 合成大小上限/取消后临时音频释放 | ✅ | `Services/EdgeTtsService.cs:38, 106–108`（MaxSourceCharacters/MaxAudioBytes 超限中止）；`TtsService` TryDelete + CleanupStaleTempFiles |
| 合成总 deadline | ⚠️ 未证实 | 大小上限明确；总时限未见专门实现 |
| 隐藏/退出遵循窗口合同 | ✅ | 面板 OnClosed → `TtsService.Stop()`（`TranslationPanelWindow.xaml.cs:1595–1607`）；A10 重启路径先停 TTS（台账 150 行） |
| 回退保留语言标签 | ✅ | 云端失败回退本地时携带 languageTag 选前缀匹配语音（`TtsService.cs:225–238`） |

## 3. 缺失子项清单（汇总，按优先建议排序）

**L1 层缺口（V2 §9 将 N01/N02/N04 列为"可靠日用"组成）：**
1. N02-IME：三入口 `GetIsInputMethodEnabled` 错误谓词未修（中文 IME 常开场景 Enter 无法提交翻译——日用高频痛点）；"Enter 换行/Ctrl+Enter 翻译"偏好缺
2. N02-来源复制：前台变化时强拉回目标窗口（与合同"停止+提示、不强抢焦点"相反）；连续相同提交无去重
3. N01-多会话仓：5 会话/2MiB/30 分钟淘汰整体未建（现单会话）；工作台草稿保护选择缺
4. N04-复制格式菜单（纯文本/Markdown 原文/仅所选）与译文/解释分别复制；"回到最新"；终态渲染保持阅读位置与选中
5. N05-交换守卫（auto 时禁用+说明，现为猜测反推）；语言搜索/最近/收藏；检测结果"未确定"反馈

**L2 层缺口：**
6. N03 全部增量：框选后确认模式、预览冻结、OCR 校对界面、上传明示、断行局部清理撤销
7. N07-历史 Service 快照字段（schema 演进）、来源/语言对/日期/仅收藏筛选、10 秒撤销、批量精确计数
8. N08 备份/导入/事务恢复整体（零实现）
9. N09-暂停/语速/语音列表与安装指引/选中朗读；本地无匹配语音静默回退默认语音
10. N06 全部（编辑 UI + 术语系统，core 侧同样无 glossary 协议）

## 4. 建议下一步（≤3 条）

1. **N02 IME 判定修正（小改、高收益、L1）**：三入口把 `GetIsInputMethodEnabled` 换成真实组词判定（`TextCompositionManager`/`ImeProcessedKey` 路径），补"Enter 行为"偏好与 LogicTests 三入口用例；顺带把划词复制的前台处理改为"核验+停止+提示"（去掉强拉 SetForegroundWindow）。
2. **N07+N04 数据与阅读小闭环（L2 先行子集）**：`TranslationHistoryEntry` 增加 Service 快照字段（写侧 `TranslationCoordinator` 已有 Provider 信息）+ 资料库筛选维度；同波补 N04 复制格式菜单（纯文本/Markdown 原文/仅所选，复用现有 MarkdownPresenter）。
3. **N01 多会话仓与 N03 确认模式排期（需设计先行）**：两者是 V2 §5 L1 剩余最大缺口，均涉内存边界（C12）与截图坐标（C10）耦合，建议先出 1 页设计小档（容量/淘汰/预览冻结策略）再排实施波次；N06/N08 维持 NOT_STARTED 记录即可。

---
*报告完。判定均附 file:line 证据；波次引用以 EXECUTION-LEDGER.md 追加记录为准。*
